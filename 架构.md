## Amplifier 坐标轴缩放边界（2026-07-01）

当前实验版按图表坐标结构区分 zoom-in / amplifier 的裁剪方式：
- `scatter` / `bubble` 是双数值轴图，amplifier crop 必须保持 x/y 等比例缩放，输出为方形局部坐标图，避免任一方向被非等比拉伸导致模型误读点中心。
- `h_bar` / `v_bar` / `line` 是“文字轴 + 数值轴”图，amplifier 的目标是找到更合适的数值刻度区间，因此只沿数值轴做局部缩放与局部 ruler 加密；文字轴只用于锁定类别/系列，不作为需要放大的测量轴。
- `h_bar` 的 crop 限制在 x 数值轴 plot 区间内，避免 y 文字轴标签进入局部图；`v_bar` 和 `line` 的 crop 限制在 y 数值轴 plot 区间内，避免 x 文字轴标签进入局部图。
- 以上调整仍然不改变机制：crop 中心来自上一轮模型预测，GT 只用于指标和日志，不用于裁剪中心或停止条件。
## Zoom-in / Amplifier 优化策略（2026-07-01）

本轮优化不改变论文机制：full flow 仍然是 `grid -> feedback -> amplifier`，baseline 仍为独立对照；amplifier 的输入仍由上一轮模型预测驱动，GT 只用于 RE/RNE 与日志复盘。优化范围仅限机制内部的图像输入质量、局部 guide 样式、裁剪窗口和提示词表达。

从 MLLM 的通用视觉编码特性出发，amplifier 图像遵循以下原则：
- 输出短边统一提升到约 `768px`，避免 224/320px 局部图在视觉编码阶段丢失 tick label、细边界和小 mark。
- 裁剪窗口不再每轮简单减半，而是采用“先保上下文、再逐步收紧”的三轮策略：第 1 轮保留候选目标周围较宽上下文，第 2 轮中等局部化，第 3 轮只在已有稳定预测附近细化。
- 局部红色 guide 与 GT grid 样式严格区分：amplifier 使用红色局部 guide；GT grid 仍使用 `#cccccc`、2/2 dash、1px。
- 局部 ruler 使用更密的插值线，但 tick label 必须保持可读；line 图采用“密线、稀疏标签”，避免红线和标签遮挡浅色折线。
- amplifier 的局部 tick/angle/radius label 框必须以对应红色 guide 为中心轴：竖线穿过 label 框中心、横线穿过 label 框中心，极坐标圆环/射线也穿过 label 框中心；贴近裁剪边缘导致无法完整显示的 label 不强行绘制。
- prompt 明确要求模型读取原图数据坐标：使用最近两条红色 guide 做比例插值，不输出 crop 像素、resize 像素或标签位置。
- 对 scatter/bubble，点大小估计只作为 crop 尺寸和可读性辅助，不作为独立预测或额外 modal call；输出仍来自 amplifier 模型回答。

各类型当前策略：
- `h_bar` / `v_bar`: amplifier crop 输出约 `768px`，局部红色 ruler 按轮次加密，窗口按预测边界周围保留足够条形边界上下文。
- `line`: amplifier crop 输出约 `768px`，目标 x 位置用红色竖向 guide 标出，y 方向用较密红线和稀疏可读标签辅助插值。
- `scatter` / `bubble`: adaptive crop 输出提升到 `768px`，局部红色 x/y 网格更密，mark diameter 只影响 crop 尺寸和 guide 间距。
- `pie`: sector amplifier 图放大到至少 `768px`，红色 start/end 边界带文本标注，帮助模型判断边界顺序。
- `donut`: 保留原有角度 crop 机制，但增加三轮 pad，上下文不再过早收窄；角度网格仍随轮次变细。
- `radar` / `rose`: polar amplifier 图提升到 `768px`，径向窗口保留更多圆心/轴向上下文，局部红色圆环标注可见 r 值。
## 实验版机制边界更新（2026-07-01）

当前实验版的最终预测和 RE/RNE 指标只允许来自模型在 grid、feedback、amplifier 三个阶段的输出；baseline 仅作为独立对照，不进入 full flow。不得使用额外的确定性图像读数来替代模型预测，也不得把这类本地读数写成阶段预测记录。允许优化的范围限于提示词、反馈图绘制、裁剪区域大小、缩放倍数和局部 guide 样式；这些优化只能服务于下一次模型输入，不能直接产出最终预测值。

grid 阶段使用 GT grid 图像及其 tick-pixel 映射作为输入；feedback 的提前停止只看模型预测之间是否一致；amplifier 最多 3 次，存在性判断必须并入 amplifier 提示与裁剪策略中，不能新增独立模型调用或本地数值读数。GT 数值只用于指标计算和日志复盘，不用于裁剪中心、提前停止或预测补全。

# 实验版项目架构

本文维护当前分支 `experiment/gt-grid-evaluation` 的实验版架构。该版本服务论文评估实验，目标是使用人工标注的 GT 类型、GT grid 图像、GT ticks/pixels 和 GT 数据，隔离自动网格生成误差，只评估第 3 步“评估预测”的能力。

## 1. 实验边界

实验版主流程不再使用：

- 上传任意图表
- 自动图表类型识别
- 自动网格生成或图像加密
- dataset preview 缓存复用

主流程固定为：

```text
选择数据集来源和图表类别
  -> 选择单个图表或全选当前类别
  -> 读取该样本的 GT config / GT grid
  -> 运行 baseline 和 GT-grid full flow
  -> 记录每次 modal call
  -> 计算 RE / RNE
```

## 2. 关键入口

| 层级 | 文件/接口 | 职责 |
|---|---|---|
| 前端工作台 | `frontend/chart-demo-ui/src/App.vue` | 快速选择图表类别、单样本或当前类别全选，并运行实验 |
| GT 实验 API | `backend/main.py` 中 `/api/gt-experiment/*` | 列样本、选样本、返回 GT 图像、运行 GT 实验 |
| 预测调度 | `backend/evaluation_prediction/service.py` | 按图表类型分发到 active runner |
| runner | `backend/evaluation_prediction/chart_modules/<type>/runner.py` | baseline、grid、feedback、zoom-in/amplifier 预测 |
| 请求日志 | `gemini_calls.py` | 记录 modal call 的 token、attempts、耗时和原始输出 |
| 实验输出 | `gt_runs/<chart_type>/<chart_name>/<run_id>/` | 先按图表类别分层，再按图表和模型 run 保存过程文件、结果 JSON、摘要日志和全量模型调用日志；旧的 `backend/evaluation_prediction/results/gt_experiments/` 只作为历史结果查找根保留 |

## 3. GT 输入约定

优先复用已有数据集配置：

```text
backend/datasets/VisHintPrompt_datasets/
  Final-RealDataset/<Category>/chart/
  Final-RealDataset/<Category>/chart_config/
  Sy.Dataset/<Category>/charts/
  Sy.Dataset/<Category>/chart_configs/
```

GT config 至少需要提供：

- `chart_id`
- `chart_type`
- `image_paths.no_grid`
- `image_paths.with_grid` 或 `image_paths.grid_with_grid`
- `data_points` / `ground_truth`
- 评估预测所需 ticks 和 pixels，例如 `x_ticks`, `x_pixels`, `y_ticks`, `y_pixels`

实验 API 直接读取这些 GT config，不再由上传图像反推类型或生成网格。

## 4. 三大核心机制

实验包含两条对照：

- `baseline`：极简提示，参考 `reference/For_bar_chart(h) - 逼近真值.py` 的 baseline 思路，并由各类型 runner 泛化。
- `full flow`：基于 GT grid 的完整机制，包括 grid、feedback、zoom-in/amplifier。

`baseline` 是独立基线方法，不是 full flow 的第 0 步。它的预测只用于 baseline 自身的 RE/RNE 和论文对比，不允许写入 full flow 的 `history`，不允许参与 feedback overlay、amplifier crop 中心或 full-flow selected prediction fallback。full flow 的历史只能由 `grid`、`feedback` 和 `amplifier` 的有效预测构成。

### 4.1 Grid：GT 网格图像提示预测

`grid` 是 full flow 的第一步，固定执行 1 次。它的核心作用是让模型直接在人工标注/确认的 `grid_with_grid` 加密网格图像上读数，从而隔离“系统自动生成网格是否准确”对评估实验的干扰。

输入包括：

- 原图对应的 `image_paths.grid_with_grid`，缺失时才兜底使用 `image_paths.with_grid` 或由 GT 网格参数临时绘制的 fallback 图。
- GT config 中的类型、ticks、pixels、tick-pixel 映射关系、对象标签和颜色等必要上下文。
- 当前处理对象，例如某个点、某根柱、某个扇区或某条序列下的类别。

实验语义约束：

- grid 阶段可以使用 GT grid 图像和 GT ticks/pixels 作为提示坐标系。
- grid 阶段不能直接把 GT 数值写进提示词让模型照抄。
- grid 阶段输出的是后续 feedback 和 zoom-in 的初始预测，而不是最终必然答案。

### 4.2 Feedback：基于上一轮预测的可视化反馈

`feedback` 是对 grid 或上一轮 feedback 结果的再判断，最多执行 2 次。它的核心作用是把模型上一轮预测以可视化方式标回图像或作为文字反馈，使模型能比较“自己的预测位置/数值”和目标对象之间的偏差。

输入包括：

- 上一轮有效预测值。
- 在 GT grid 图像上绘制的预测 overlay，或等价的文字反馈。
- 最近一到两轮预测历史，用于提醒模型修正方向。

自适应规则：

- feedback 至少尝试 1 次，最多 2 次。
- 如果第 1 轮 feedback 预测与 grid 预测一致，则停止 feedback。
- 如果第 2 轮 feedback 预测与第 1 轮 feedback 预测一致，则停止 feedback。
- 如果上一轮没有有效预测，可以记录失败并跳过依赖该预测的后续反馈图生成。

实验语义约束：

- feedback 不能使用 GT 作为生成端提前停止依据；GT 只在该阶段结束后用于指标计算和日志复盘。
- feedback 不能使用 GT 来生成下一轮预测内容。
- feedback 的 overlay 中只能画模型已经产生的预测，不应画 GT 真值位置。

### 4.3 Zoom-in / Amplifier：围绕预测区域的局部放大

`zoom-in` 或 `amplifier` 是 full flow 的局部精修阶段，最多执行 3 次。它的核心作用是基于上一轮预测生成局部裁剪图，让模型在更高视觉分辨率下重新估计目标值。

输入包括：

- feedback 或上一轮 amplifier 的有效预测。
- 基于该预测中心生成的局部 crop。
- crop 内可见的局部 ticks、局部坐标范围或局部角度/半径提示。
- 最近预测历史，用于提醒模型不要重复明显偏差。

自适应规则：

- amplifier 至少尝试 1 次，最多 3 次。
- 如果当前 amplifier 预测与上一轮用于生成 crop 的模型预测一致，则停止 zoom-in。
- 如果没有可用的前序预测，原则上应跳过 amplifier 或回退到非 GT 的中性中心；不能用 GT 数值作为裁剪中心。
- 存在性判断并入 amplifier 本身：同一次模型调用先判断目标是否可读，再输出预测值。
- 如果目标不可见或无法确认，模型按同一 JSON 契约返回 `readable=false`，预测值字段使用 `null`，该轮计入 amplifier 次数；下一轮基于同一预测中心反向扩大 ROI，最大扩大到上一轮 ROI 的 2 倍。
- `attempts` 只表示网络/API 重试；不可见后的扩大 ROI 不是 attempts，而是下一次 amplifier 轮次。

实验语义约束：

- amplifier 的裁剪中心应来自模型预测或几何先验，不能来自 GT 数值。
- GT 只能用于阶段结束后的 RE/RNE 计算和日志复盘。
- 对论文实验而言，任何“用 GT 帮模型看见正确区域”的实现都属于泄漏，需要避免或在日志中显式标注。

### 4.4 Full Flow 轮次上限

- `grid` 固定执行 1 次。
- `feedback` 至少 1 次，最多 2 次；如果当前预测与 grid 或上一轮 feedback 预测一致，则停止 feedback。
- `zoom-in/amplifier` 至少 1 次，最多 3 次；如果当前预测与上一轮模型预测一致，则停止 zoom-in；如果当前轮返回 `readable=false`，下一轮扩大 ROI 后继续，直到三轮上限。

因此 full flow 最好情况是 3 次预测调用：

```text
grid 1 次 -> feedback 1 次稳定 -> zoom-in 1 次稳定
```

最坏情况是 6 次预测调用：

```text
grid 1 次 -> feedback 2 次 -> zoom-in 3 次
```

当前 GT 实验预测稳定性阈值按坐标轴范围或预测量级归一化误差 `0.25%` 判定，可通过环境变量调整：

- `CHART_PREDICTION_CONSISTENCY_TOLERANCE=0.0025`

`CHART_MATCH_RNE_TOLERANCE` 仍可用于指标审计或兼容函数，但不作为 grid/feedback/amplifier 的生成端停止条件。

## 5. 提示词与参数设置

本节单独维护三大机制涉及的 prompt 类型、图像输入、关键参数和不同图表类型差异。实际完整提示词以各模块 `prompts.py` 或 `polar_value.py` 为准；这里记录实验语义和可复现实验所需参数。

### 5.0 模型视觉预算注册表

实验版新增模型视觉预算注册表：

```text
backend/evaluation_prediction/common/model_vision_registry.py
```

它不改变论文机制，不新增模型调用，也不直接产生预测值；只在 zoom-in/amplifier 阶段根据当前 MLLM 的图像编码特性，选择局部 crop 的输出尺寸、最大边长、label padding、三轮 crop 上下文收缩曲线、局部 tick 加密密度和 polar crop 窗口。核心原则是：让模型看清局部 tick/边界/mark，同时避免把局部图无差别放大到浪费视觉 token 的尺寸。

amplifier 同时控制两件事：

- `source ROI`：从原图中裁剪出来的真实局部区域，决定画面里还保留多少干扰对象。
- `output size`：把 source ROI 上采样后的输入图尺寸，决定 MLLM 能否看清局部边界、tick label 和红色 guide。

二者不能混为一谈。当前实现会先限制 source ROI 在数值 plot 区域中的占比，再按模型视觉预算上采样到目标尺寸。默认 source ROI 上限为第 1/2/3 轮分别不超过数值轴 plot span 的约 `32% / 24% / 18%`；如果上一轮 amplifier 返回不可读，下一轮才按 `roi_scale` 扩大，但仍有 `55%` 的硬上限，避免退化成接近全图裁剪。这样可以减少干扰对象，同时不增加 modal call，也不浪费图像 token。

当前注册的论文模型：

| 模型 | point | ruler | sector/polar | max side | tick divisors | 策略 |
|---|---:|---:|---:|---:|---|---|
| `GPT-4o` | 768 | 640 | 768 | 1024 | 4/6/8 | GPT-4o 高细节图像按 512 tile 档计，ruler 图用 640+64 pad 避免外部 label 触发额外 tile。 |
| `Gemini 2.5 Flash` | 768 | 640 | 768 | 768 | 4/6/8 | Gemini 大图按 768 tile 档计，point 图吃满单 tile，ruler 图控制在 768 内。 |
| `claude-haiku-4.5` | 640 | 576 | 640 | 896 | 3/5/6 | Claude 图像 token 近似按像素面积增长，减少面积和 dense guide。 |
| `Intern VL3-78B` | 896 | 672 | 896 | 896 | 3/4/6 | InternVL3 使用 448 tile 动态切图，控制在 2x2 tile 级别，保留文字可读性。 |
| `Pixtral-12B-2409` | 640 | 512 | 640 | 768 | 3/4/5 | Pixtral 原生 16x16 patch，token 近似随面积增长；小模型避免大图和密集 guide。 |

可通过环境变量覆盖：

```text
CHART_AMPLIFIER_VISION_PROFILE
CHART_AMPLIFIER_TARGET_SIDE
CHART_AMPLIFIER_POINT_TARGET_SIDE
CHART_AMPLIFIER_RULER_TARGET_SIDE
CHART_AMPLIFIER_SECTOR_TARGET_SIDE
CHART_AMPLIFIER_POLAR_TARGET_SIDE
CHART_AMPLIFIER_MAX_SIDE
CHART_AMPLIFIER_LABEL_PAD
CHART_AMPLIFIER_WINDOW_SCHEDULE
CHART_AMPLIFIER_TICK_DIVISORS
CHART_AMPLIFIER_POINT_GRID_DENSITY
CHART_AMPLIFIER_POLAR_WINDOW_FACTORS
CHART_AMPLIFIER_POLAR_MIN_WINDOW
```

GT 实验 API 返回的 payload 会记录 `model_vision_profile`，用于结果复盘。`modal_call_records.csv` 还会写入 `image_width`、`image_height`、`estimated_image_tokens` 和 `vision_profile`，用于审计每次 amplifier 输入是否跨了不必要的视觉 token 档。

### 5.1 统一轮次与运行参数

| 参数 | 当前设置 | 来源 | 含义 |
|---|---:|---|---|
| `CHART_EXPERIMENT_MODE` | `gt` | `/api/gt-experiment/run/` | 启用 GT 实验轮次控制 |
| `CHART_EXPERIMENT_PRESERVE_GT` | `1` | `/api/gt-experiment/run/` | runner 读取 GT `data_points` 作为评估目标 |
| `CHART_FEEDBACK_ROUNDS` | `2` | `/api/gt-experiment/run/` | feedback 最大轮次 |
| `CHART_AMPLIFIER_ROUNDS` | `3` | `/api/gt-experiment/run/` | zoom-in/amplifier 最大轮次 |
| `CHART_BAR_AMPLIFIER_ROUNDS` | `3` | `/api/gt-experiment/run/` | h/v bar amplifier 最大轮次 |
| `CHART_PREDICTION_CONSISTENCY_TOLERANCE` | `0.0025` | `/api/gt-experiment/run/` | 判断两轮模型预测是否稳定一致，不使用 GT/RNE |
| `CHART_MATCH_RNE_TOLERANCE` | `0.01` | `common/runtime.py` | 仅用于指标审计/兼容函数，不作为生成端退出依据 |
| `MLLM_MAX_RETRIES` | 默认 `3` | `gemini_calls.py` | 网络/API 重试次数，记录为 `attempts`，不计入实验轮次 |
| `MLLM_TIMEOUT_SECONDS` | 默认 `180` | `gemini_calls.py` | 单次模型请求总超时 |
| `CHART_MODEL_PROFILE` | 默认 `gemini` | `common/model_config.py` | 模型配置 profile |
| `CHART_BASE_URL` / `CHART_MODEL_NAME` / `CHART_API_KEY` | 可覆盖 | `common/model_config.py` | 模型服务地址、模型名、密钥 |

### 5.2 直角坐标 Bar：`v_bar` / `h_bar`

| 机制 | `prompt_type` | 图像输入 | 关键提示词信息 | 关键参数 |
|---|---|---|---|---|
| Grid | `grid` | `grid_with_grid` | 给出 x/y ticks、series color，要求定位目标柱/堆叠段并按网格线插值。`v_bar` 读 y 顶边或堆叠段 top-bottom；`h_bar` 读 x 右边界或堆叠段 right-left。 | `x_ticks`, `y_ticks`, `series_color`, `chart_type` |
| Feedback | `feedback` | 第 1 轮通常仍是 `grid_with_grid`；后续轮使用带红色 crosshair/overlay 的反馈图 | 提示上一轮预测值和红色标记，让模型比较标记与真实边界并修正。 | `pred_feedback=history[-2:]`, `feedback_round=2`, `current_round` |
| Zoom-in / Amplifier | `amplifier` | 基于预测中心裁剪出的局部图 | crop 两侧绘制局部数值轴和网格；要求在局部 ticks 间线性插值。 | `visible_ticks`, `current_round=amp_round`, `amplifier_rounds=3` |

bar 的 amplifier crop 不再使用额外模型判断 `crop` 是否包含目标。存在性判断与数值提取合并在 `amplifier` 提示词内：可读则返回数值；不可读则按同一 JSON 契约返回 `readable=false`，预测值字段使用 `null`。runner 会记录该轮，并在下一轮把 ROI 反向扩大，最大扩大到上一轮的 2 倍。

`h_bar` 的 amplifier crop 使用原图 `no_grid` 作为底图，再叠加红色局部虚线网格和底部 tick ruler；这样局部放大阶段只呈现红色过程 guide，不把 GT grid 的灰色加密 label 裁进来干扰读数。局部 crop 会提高输出分辨率，并要求模型在相邻 red ruler ticks 之间按比例插值：只有边界精确落在线上才返回 tick 值，若边界位于两条红线之间则返回对应的中间数据值。

文件位置：

- `backend/evaluation_prediction/chart_modules/v_bar/prompts.py`
- `backend/evaluation_prediction/chart_modules/h_bar/prompts.py`
- `backend/evaluation_prediction/chart_modules/v_bar/model.py`
- `backend/evaluation_prediction/chart_modules/h_bar/model.py`

模型调用参数：

- `max_tokens=512`
- temperature 未在 bar model 显式传入，使用统一模型请求层默认值。
- timeout 使用 model client timeout。

图表类型差异：

- `v_bar` 的核心数值轴是 y；输出 JSON 为 `{"datapoints": [{"series, x_label": ["x_label", y]}]}`。
- `h_bar` 的核心数值轴是 x；输出 JSON 为 `{"datapoints": [{"series, y_label": [x, "y_label"]}]}`。
- 实验版处理对象不包括 `v_stacked_bar` 和 `h_stacked_bar`；如果旧代码中仍有 stacked helper，那只是未启用的内部兼容路径，不属于本论文实验范围。

### 5.3 Line

| 机制 | `prompt_type` | 图像输入 | 关键提示词信息 | 关键参数 |
|---|---|---|---|---|
| Grid | `grid` | `grid_with_grid` | 给出 x/y ticks 和 series color，要求在指定 x 类别处定位折线点，并在最近水平网格线之间插值 y 值。 | `x_ticks`, `y_ticks`, `series_color` |
| Feedback | `feedback` | 带上一轮预测 marker 的 `grid_with_grid` overlay | 提示上一轮预测点 `(x, y)`，让模型比较 marker 与真实折线点并修正 y。 | `pred_feedback=history[-1]` |
| Zoom-in / Amplifier | `amplifier` | 围绕前序预测 y 值裁剪的局部图 | crop 包含目标 x 类别和局部 y ticks，要求在 crop 中定位该 series 的折线点。 | `visible_ticks`, `round_index` |

line 的 amplifier crop 同样不再使用额外 `amplifier_exists` modal call。局部图只由上一轮 y 预测确定；如果模型在同一次 amplifier 预测中判断目标点不可读，则返回 `readable=false` 且 y 为 `null`，下一轮扩大 ROI 后继续。

文件位置：

- `backend/evaluation_prediction/chart_modules/line/prompts.py`
- `backend/evaluation_prediction/chart_modules/line/model.py`

模型调用参数：

- `max_tokens=512`
- temperature 未显式传入，使用统一模型请求层默认值。

当前注意事项：

- line 的 amplifier crop 中心应来自 `feedback_pred[1]` 或非 GT 中性中心；不能用 `target.gt_y` 作为裁剪中心。

### 5.4 Point Charts：`scatter` / `bubble`

| 机制 | `prompt_type` | 图像输入 | 关键提示词信息 | 关键参数 |
|---|---|---|---|---|
| Grid | `grid` | `grid_with_grid` | 给出 x/y ticks，要求读取目标 circle/bubble 的视觉中心数据坐标，不读标签位置。 | `x_ticks`, `y_ticks`, `mark_name`, `visual_name` |
| Feedback | `feedback` | 带上一轮预测 marker 的 `grid_with_grid` overlay | 提示上一轮预测 `(x, y)`，要求比较 marker 与目标中心并修正。 | `pred_feedback=history[-1]` |
| Zoom-in / Amplifier | `feedback_crop_adaptive`，日志 stage 记为 `amplifier` | 基于 `feedback_pred` 生成的局部 crop，可带局部网格 | 提醒模型只用 crop 中可见红色 tick/grid，返回原图数据坐标，不返回 crop 像素坐标；同一次提示词中嵌入目标点/气泡的近似像素直径，辅助判断目标是否可读。 | `feedback_pred`, `feedback_round=run_idx`, `x/y local ticks`, `estimated_mark_diameter_px` |

scatter/bubble 的 adaptive crop 不再通过模型估计点直径，也不再用模型单独判断 crop 是否包含目标点。当前实现先基于上一轮 feedback 预测坐标和原图 `no_grid` 像素，确定性估计附近 circle/bubble 的直径；优先使用彩色/深色连通域，浅色或边缘碎片较多的 bubble 会回退到径向边缘估计。该估计只影响 amplifier 的局部 crop 尺度，并作为同一次 amplifier 提示词里的识别/可读性线索，不要求模型输出直径，也不产生额外 modal call。如果同一次 amplifier 预测返回 `readable=false` 且坐标为 `null`，下一轮扩大 crop 窗口后继续。这样每次 modal call 都对应可评价的 baseline/grid/feedback/amplifier 预测。

点大小估计的记录字段会写入 runner CSV 和 modal call context 的 `stage_support`，包括：

- `estimated_mark_diameter_px`
- `mark_size_source`
- `mark_size_confidence`
- `adaptive_crop_size_px`
- `adaptive_crop_expanded`

文件位置：

- `backend/evaluation_prediction/chart_modules/scatter/prompts.py`
- `backend/evaluation_prediction/chart_modules/scatter/model.py`
- `backend/evaluation_prediction/chart_modules/bubble/prompts.py` 复用 scatter prompt
- `backend/evaluation_prediction/chart_modules/bubble/model.py`

模型调用参数：

- `max_tokens=512`
- `max_completion_tokens=512`
- `temperature=0`
- `response_format={"type": "json_object"}`
- system prompt 强制 JSON-only。

图表类型差异：

- scatter 的 `mark_name` 为 `circle`。
- bubble 的 `mark_name` 为 `bubble`，核心坐标预测 prompt 与 scatter 共用，但 crop 尺度会按估计直径保留更大的 bubble 可视范围。
- adaptive crop 失败或不可读时记录 `readable=false` / `null` 预测语义，不用 `(-1, -1)` 这类可能与负数图表冲突的数值哨兵，也不借助 GT 重新定位。

### 5.5 Pie / Donut

当前 pie 和 donut 已收敛为 GT 实验版的统一对象级流程：每个 GT 扇区先执行 1 次 `baseline` 作为论文对照组，然后执行 full flow：`grid` 固定 1 次、`feedback` 最多 2 次、`amplifier` 最多 3 次。GT 只用于 RE/RNE 计算和日志复盘，不用于生成角度预测、裁剪中心或提前停止。

| 机制 | `prompt_type` | 图像输入 | 关键提示词信息 | 关键参数 |
|---|---|---|---|---|
| Baseline | `baseline` | `no_grid` | 不使用 grid，直接估计目标扇区比例，作为极简提示对照。 | sector label |
| Grid | `grid` | `with_grid` / `grid_with_grid` | 使用角度参考线估计目标扇区 `start_angle`、`end_angle`，并换算比例。 | `theta_ticks`, label order, sector label |
| Feedback | `feedback` | 带红色 start/end boundary 与 arc 的反馈图 | 提示上一轮 `start_angle/end_angle`，要求比较红色边界与真实扇区边界，必要时修正或交换顺序。 | `prev_angle`, feedback image |
| Zoom-in / Amplifier | `amplifier` | `crop_sector_for_amplifier(...)` 生成的局部扇区 crop | 提示上一轮角度定义的候选范围、局部绘制的 radial tick angles，要求精修 `start_angle/end_angle`。 | `prev_angle`, `drawn_angles`, `angle_order_hint` |

文件位置：

- `backend/evaluation_prediction/chart_modules/pie/prompts.py`
- `backend/evaluation_prediction/chart_modules/donut/prompts.py`
- `backend/evaluation_prediction/chart_modules/pie/runner.py`
- `backend/evaluation_prediction/chart_modules/donut/runner.py`
- `backend/evaluation_prediction/chart_modules/pie/visual.py`
- `backend/evaluation_prediction/chart_modules/donut/visual.py`

模型调用参数：

- 目标扇区调用：`max_tokens=2048`, `temperature=0.0`
- 旧 whole-chart 解析代码仍保留为兼容 fallback，但 GT 实验对象级流程默认不依赖 whole-chart 预抽取。

局部放大参数：

- `crop_sector_for_amplifier` 默认按轮次收紧：第 1 轮 `pad=15.0, grid=5°, zoom=2.0`；第 2 轮 `pad=9.0, grid=3°, zoom=2.0`；第 3 轮 `pad=6.0, grid=2°, zoom=3.0`。
- `draw_angle_feedback` 默认 `grid_line_ratio=0.1`。
- amplifier prompt 会注入 `drawn_angles` 和可选 `angle_order_hint`。

图表类型差异：

- pie 使用轻量局部扇区 crop：根据上一轮模型角度预测在原图上裁剪目标候选扇区，不额外调用模型判断 crop 是否包含目标。
- donut 使用 `donut/visual.py` 中的 refined crop：会根据上一轮模型角度预测生成局部扇区 crop，并可带 `drawn_angles` 与 `angle_order_hint`。
- 两者都不允许用 GT 扇区角度作为 feedback 或 amplifier 的绘图/裁剪依据。

### 5.6 Radar / Rose

当前 radar/rose 走 `backend/evaluation_prediction/chart_modules/polar_value.py`，并通过 `backend/evaluation_prediction/chart_modules/polar_visual.py` 接入完整三机制。每个 GT 目标先执行 1 次 `baseline`，再执行 `grid -> feedback -> amplifier`。feedback 图和 amplifier crop 都围绕上一轮模型预测的 `pred_r` 绘制，不围绕 GT 半径绘制。

| 机制 | `prompt_type` | 图像输入 | 关键提示词信息 | 关键参数 |
|---|---|---|---|---|
| Baseline | `baseline` | `no_grid` | 不依赖 GT grid，从可见原图估计 radial value。 | 同上 |
| Grid | `grid` | `grid_with_grid`，缺失时即时绘制 GT grid | 给出 `r_ticks`、角度轴标签、角度位置和颜色，要求按径向网格插值 radial value。 | `r_ticks`, `theta_ticks`, `theta_angles`, `series_color` |
| Feedback | `feedback` | `polar_visual.draw_polar_feedback(...)` 生成的红色预测标记图 | 提示上一轮 `pred_r`，要求比较红色径向标记与真实目标点/边界并修正 radial value。 | `prev_r`, predicted radial marker |
| Zoom-in / Amplifier | `amplifier` | `polar_visual.crop_polar_amplifier(...)` 生成的局部 crop | crop 围绕上一轮模型 `pred_r` 在对应角度轴上的位置生成，提示可见局部 radial ticks 并要求精修 r 值。 | `prev_r`, `visible_ticks`, local radial guides |

模型调用参数：

- `max_tokens=8192`
- `temperature=0.0`
- timeout `300s`
- 可用 `POLAR_CHART_MODEL_NAME` 覆盖模型名。

图表类型差异：

- radar 的处理对象通常是 `series_name, theta_label`，需要同时定位系列颜色和角度轴。
- rose 的处理对象通常是单个 sector label，主要估计该扇区外边界的 radial value。
- 如果上一轮没有有效 `pred_r`，对应 feedback/amplifier 会写入空预测记录，不会用 GT 半径补位。

## 6. 指标

实验版输出两类误差：

- `RE`: `abs(pred - gt) / max(abs(gt), eps)`
- `RNE`: `abs(pred - gt) / axis_range`

对于 scatter/bubble 等二维对象，会分别计算 x/y，并给出可用维度的平均值。对于条形图和折线图，主要数值轴参与 RE/RNE。

## 7. Modal Call 日志

每次模型请求写入：

```text
gt_runs/<chart_type>/<chart_name>/<run_id>/modal_calls.jsonl
gt_runs/<chart_type>/<chart_name>/<run_id>/modal_calls_full.jsonl
```

`modal_calls.jsonl` 是摘要日志，每条记录至少包含：

- `call_id`
- `chart_name`
- `processing_object`
- `object_category`
- `gt`
- `stage`
- `round`
- `attempts`
- `input_tokens`
- `output_tokens`
- `total_tokens`
- `text_prompt_tokens`
- `request_duration_ms`
- `raw_prediction`
- `image_path`
- `success`

`modal_calls_full.jsonl` 是全量日志，用于论文实验复盘，额外保存：

- 完整 `request_payload`
- 完整 `response_json`
- 提取出的 `response_text`
- 请求 URL 列表
- 每次网络失败、HTTP 错误或重试的 `retry_events`

其中 `attempts` 只表示网络波动或接口失败后的重试次数，不计入 grid/feedback/zoom-in 的实验轮次。

`call_id` 由统一模型请求层在每次 modal call 发起时生成，并同时写入摘要日志和全量日志。runner 在解析模型输出并写入结构化预测记录时读取同一个 `call_id`，因此 `modal_calls*.jsonl`、runner 的结构化 CSV、`gt_metric_records.csv` 和 `*_enriched.jsonl` 可以显式关联。旧的 chart/object/stage/round/image_path 匹配只作为历史日志的兜底策略。

## 8. 当前状态

已完成：

- 前端实验对象选择：类别筛选、单样本选择、当前类别全选、批量运行。
- 新增 `/api/gt-experiment/categories/`、`/samples/`、`/select/`、`/image/`、`/run/`。
- GT config 在实验模式下保留 `data_points`；旧上传流程仍默认剥离 GT。
- GT 实验输出按 `gt_runs/<chart_type>/<chart_name>/<run_id>/` 分目录保存，并在每个 run 内维护 `pf/` 作为过程文件目录，避免大量样本直接堆在根目录，也避免 Windows 路径过深。
- synthetic 数据集已兼容 `charts/<type>/`、`charts/`、`chart/` 等不同图像目录写法。
- 全部实验 runner 已支持统一机制契约：baseline 1、grid 1、feedback 1-2、amplifier 1-3。
- modal call 摘要日志和全量输入输出日志已接入统一模型请求层。
- modal call 原始输出、runner 结构化预测、GT 指标 CSV 和 enriched 日志已通过 `call_id` 显式关联。
- 实验版处理对象仅包含 `bubble`、`scatter`、`line`、`v_bar`、`h_bar`、`pie`、`donut`、`radar`、`rose`；不包含 `v_stacked_bar` 和 `h_stacked_bar`。

后续建议：

- 增加论文批量实验汇总导出 CSV/Markdown。

## 9. 2026-07-01 实验机制收口

本轮收口后的实验版以 `backend/evaluation_prediction/common/experiment_contract.py` 作为图表类型实验契约的静态入口。每个图表类型都明确声明 runner、prompt、visual、model 所在模块，并统一要求包含：

- `baseline`：论文对照组，不属于 full flow 的自适应阶段。
- `grid`：full flow 第一步，固定 1 次，输入 GT grid 图像。
- `feedback`：基于上一轮模型预测生成反馈图或反馈提示，最多 2 次。
- `amplifier`：基于上一轮模型预测生成局部放大图，最多 3 次。

公共实验审计逻辑已拆到 `backend/evaluation_prediction/common/experiment_flow.py`，负责统一阶段命名、生成 `stage_coverage.json`、写出 `gt_metrics.json` 和 `gt_metric_records.csv`。`backend/main.py` 现在只负责 GT 实验 API 编排，不再承载阶段覆盖和指标文件落地的核心逻辑。

各类型核心模块边界如下：

| 类型 | runner | prompt | visual | model |
|---|---|---|---|---|
| scatter | `chart_modules/scatter/runner.py` | `scatter/prompts.py` | `scatter/visual.py` | `scatter/model.py` |
| bubble | `chart_modules/bubble/runner.py` | `bubble/prompts.py` | `bubble/visual.py` | `bubble/model.py` |
| line | `chart_modules/line/runner.py` | `line/prompts.py` | `line/visual.py` | `line/model.py` |
| v_bar | `chart_modules/v_bar/runner.py` | `v_bar/prompts.py` | `v_bar/visual.py` | `v_bar/model.py` |
| h_bar | `chart_modules/h_bar/runner.py` | `h_bar/prompts.py` | `h_bar/visual.py` | `h_bar/model.py` |
| pie | `chart_modules/pie/runner.py` | `pie/prompts.py` | `pie/visual.py` | `pie/model.py` |
| donut | `chart_modules/donut/runner.py` | `donut/prompts.py` | `donut/visual.py` | `donut/model.py` |
| radar | `chart_modules/radar/runner.py` | `polar_value.py` | `polar_visual.py` | `polar_value.py` |
| rose | `chart_modules/rose/runner.py` | `polar_value.py` | `polar_visual.py` | `polar_value.py` |

特别是 radar/rose 已从 baseline/grid 扩展为完整三机制：`polar_visual.py` 会根据上一轮模型预测的 `pred_r` 画反馈标记和生成局部 crop。该过程不使用 GT 作为定位、裁剪中心或提前停止依据；GT 只用于 RE/RNE 计算和日志复盘。

当前单对象实验预算统一为：

```text
baseline 1 次
grid 1 次
feedback 1-2 次
amplifier 1-3 次
```

其中 `baseline` 是论文对照组；真正的 full flow 是 `grid -> feedback -> amplifier`。grid 固定执行 1 次；feedback 和 amplifier 至少执行 1 次，后续轮次根据模型预测是否稳定一致提前停止。baseline 结果不能作为 full flow 后续阶段的输入，也不能在 full flow 缺失时被选作 full-flow 结果。

如果某阶段因上一轮没有有效预测而无法生成反馈图或 crop，应记录失败/缺失，不允许用 GT 值替代上一轮预测来继续生成图像。

### 9.1 各类型三机制执行速览

| 类型 | Baseline | Grid | Feedback | Amplifier |
|---|---|---|---|---|
| `v_bar` | `no_grid` 上估计目标柱/堆叠段数值 | `grid_with_grid` 上按 y 轴网格插值 | 把上一轮预测边界/数值画回 grid 图，模型修正 y 或 segment value | 围绕上一轮预测值裁剪局部 y 区间，绘制局部 ticks/grid 后重估 |
| `h_bar` | `no_grid` 上估计目标柱/堆叠段数值 | `grid_with_grid` 上按 x 轴网格插值 | 把上一轮预测边界/数值画回 grid 图，模型修正 x 或 segment value | 围绕上一轮预测值裁剪局部 x 区间，绘制局部 ticks/grid 后重估 |
| `line` | `no_grid` 上估计指定 series 在指定 x 处的 y 值 | `grid_with_grid` 上按 y 轴网格插值 | 把上一轮预测点 marker 画回 grid 图，模型比较 marker 与折线点 | 围绕上一轮预测 y 值裁剪局部窗口，并在局部 ticks 中重估 |
| `scatter` | `no_grid` 上估计目标点中心坐标 | `grid_with_grid` 上按 x/y 网格插值 | 把上一轮预测点 marker 画回 grid 图，模型修正坐标 | 围绕上一轮 feedback 坐标生成 adaptive crop，局部网格提示返回原图坐标 |
| `bubble` | 同 scatter，但目标 mark 是 bubble | 同 scatter | 同 scatter | 同 scatter，使用确定性默认 mark 窗口；不可读时下一轮扩大 ROI |
| `pie` | `no_grid` 上估计目标扇区比例 | `grid_with_grid` 上估计目标扇区 `start_angle/end_angle` | 按上一轮预测角度绘制红色边界和 arc，模型修正边界 | 基于上一轮预测角度裁剪目标候选扇区局部图，模型精修角度 |
| `donut` | `no_grid` 上估计目标 ring sector 比例 | `grid_with_grid` 上估计 `start_angle/end_angle` | 按上一轮预测角度绘制红色边界和 arc，模型修正边界/顺序 | 基于上一轮预测角度生成 donut sector crop，并注入局部角度 guides |
| `radar` | `no_grid` 上估计某 series 在某轴上的 radial value | `grid_with_grid` 上按 `r_ticks` 插值 | 根据上一轮 `pred_r` 在对应角度轴画红色径向标记，模型修正 r | 围绕上一轮 `pred_r` 在该轴上的位置生成局部 crop，按局部 radial guides 重估 |
| `rose` | `no_grid` 上估计某 sector 外边界 radial value | `grid_with_grid` 上按 `r_ticks` 插值 | 根据上一轮 `pred_r` 在 sector 方向画红色径向标记，模型修正 r | 围绕上一轮 `pred_r` 的 sector 边界位置生成局部 crop，按局部 radial guides 重估 |

所有类型都遵循同一条约束：feedback 和 amplifier 的图像生成只能使用模型上一轮预测或中性几何先验，不能使用 GT 数值作为下一轮提示图或 crop 的定位依据。

## 10. 图像样式边界

实验版中必须区分两套视觉样式：

- `grid_with_grid` / fallback GT 网格图：只用于 grid 阶段和中列预览，使用灰色 `#cccccc`、`2px` 实线 + `2px` 空白、`1px` 线宽，且只给加密对象补 label。
- feedback / zoom-in / amplifier 过程图：属于模型预测过程文件，沿用最初实验里的红色预测标记和红色局部 guide。bar/line/point 的局部 crop 使用红色局部虚线网格；其中 `h_bar` amplifier crop 以 `no_grid` 原图为底图，再叠加底部局部 tick ruler，避免灰色 GT grid label 和红色局部 guide 混在一起。pie/donut 使用红色角度边界和 arc；radar/rose 使用红色径向/圆环 guide。

这两套样式不能互相替换：GT 网格图不能出现红色预测 guide；amplifier crop 也不应被渲染成 `#cccccc` 的 GT 网格样式。

## 11. `grid_with_grid` 与缺图 fallback

实验版的 grid 阶段以 `grid_with_grid` 为准，也就是加密后的网格版本。系统优先读取 GT config 中已有的 `image_paths.grid_with_grid`；如果该文件不存在，才依次兜底到 `image_paths.with_grid`、`encrypted_grid_path` 或基于 GT JSON 中的网格参数即时绘制的 fallback 图。

因此 `backend/evaluation_prediction/common/gt_grid_renderer.py` 不是 grid 阶段的首选来源，而是“仓库中没有网格图时”的补图机制。fallback 图表达 grid 阶段需要的网格坐标系；只有加密对象才绘制对应 tick label，不表达模型预测或反馈过程。当前 fallback 样式为：

- 颜色：`#cccccc`，即 RGB `(204, 204, 204)`。
- 虚线：`2px` 实线 + `2px` 空白循环，等价于 Matplotlib `(0, (2, 2))`。
- 线宽：`1px`。
- 网格线覆盖整张图像画布，避免模型因局部线段长度误解网格范围。
- 网格线优先使用 `*_pixels_encrypted`；缺失时可用普通 `*_pixels` 兜底补线。
- tick label 只绘制加密得到的 tick-value。当前 GT grid 的加密规律是：在每两个原始相邻 tick 中间插入一个加密 tick，因此最终序列中两端以及偶数位 tick 视为原始 tick，奇数位中点 tick 视为加密 tick；fallback renderer 只给这些中点 tick 绘制 label。
- Cartesian 图的加密 x/y tick label 优先在原图轴侧区域寻找已有同轴 label 的暗像素主行/主列：x label 对齐到原图 x 轴 label 的中心行，y label 对齐到原图 y 轴 label 的中心列。只有推断失败时，才回退到 tick-pixel 几何关系和轴侧留白。
- Polar 图的径向 `r_ticks` 也按“原始 tick 两两中间插入”的规则识别加密 label；普通角度轴 label 不因缺图 fallback 额外补绘。
- 不绘制红色预测标记、加粗外框或局部 zoom-in 辅助标记。

当前 GT JSON 通常不包含发布版 OCR label 框、轴侧边界像素或字体 style。为减少 label 和同轴既有 label 不在一条线上的问题，fallback renderer 会先用底图像素推断轴侧文字带，再绘制加密对象的 label；如果后续 GT JSON 补充 `encrypted_label_styles`、OCR box 或显式轴侧边界，renderer 应优先使用这些标注来复原发布版轴侧 OCR 的中心区域布局。

绘制输入只使用实验允许公开/使用的结构化标注参数：

- 直角坐标图：`x_ticks`, `x_pixels`, `y_ticks`, `y_pixels`。
- 极坐标/圆形图：`center`, `r_pixels`/`radius`, `theta_angles`/`axes_angles`/`theta_ticks`。
- 底图优先使用 `image_paths.no_grid` 或 `image_path`；如果 JSON 只有 `with_grid`，则使用该图作为绘制底图。

生成路径统一缓存到：

```text
backend/evaluation_prediction/results/gt_rendered_grids/<dataset_or_group>/<chart_id>/<chart_id>_gt_grid_grid_with_grid_midpoint_encrypted_labels_cccccc_dash_2_2_w1.png
```

接入位置：

- `/api/gt-experiment/image/{sample_id}/grid` 通过 `resolve_gt_image_path(...)` 优先返回已有 `grid_with_grid` / `with_grid`，缺失时才返回即时绘制结果。
- Cartesian/point/bar/line 的通用 loader 通过 `common/backend_dataset.py` 优先填充已有 `image_paths.grid_with_grid`。
- pie/donut 的独立 loader 通过各自 `data.py` 优先填充已有 `image_paths.grid_with_grid`。
- radar/rose 的 polar loader 通过 `polar_value.py` 优先填充已有 `image_paths.grid_with_grid`。

zoom-in/amplifier 阶段仍然可以生成红色局部 guide、局部 ticks 或预测 marker，但这些属于过程文件，不应出现在中列的 grid 预览中。GT 数值仍只用于 RE/RNE 和日志复盘，不用于生成模型预测值、提前停止或 zoom-in 裁剪中心。

## 12. 完整性测试

实验版提供一个不调用模型的流程完整性检查脚本：

```powershell
python backend/evaluation_prediction/gt_experiment_flow_integrity_check.py --source synthetic
```

该脚本对 `bubble`、`scatter`、`line`、`v_bar`、`h_bar`、`pie`、`donut`、`radar`、`rose` 各取一个样本，实际检查：

- GT JSON 是否可解析，目标对象是否可枚举。
- `grid_with_grid` 是否存在；缺失时 fallback renderer 是否能生成 GT 网格图，且仅对加密对象绘制 label。
- baseline、grid、feedback、amplifier 四类提示词是否能生成。
- Cartesian 和 polar value 类 prompt 是否包含 GT tick-pixel 映射；pie/donut 是否包含角度参考网格说明。
- feedback overlay 和 amplifier crop 是否能生成过程文件。
- GT 模式轮次是否为 baseline/grid 1 次、feedback 最多 2 次、amplifier 最多 3 次。
- 生成端是否避免用 GT/RNE 作为提前停止条件，point chart 的 amplifier crop center 是否避免回退到 GT。
- 实验入口是否排除 `v_stacked_bar` / `h_stacked_bar`，以及 modal call 与结构化预测是否具备 `call_id` 显式关联。

最近一次 synthetic 九类完整性测试全部通过，报告位置：

```text
backend/evaluation_prediction/gt_experiment_flow_integrity_report_zh.md
```

## 13. 2026-07-01 整合审查结论

本节记录当前实验版按第一性原理重新审查后的系统闭环状态。审查目标不是确认“接口能跑”，而是确认论文实验语义成立：GT 只消除网格生成误差，不把答案泄漏给模型；每类图表都执行同一套机制；每次模型调用都能复盘。

### 13.1 输入闭环

当前 GT 实验入口只接收数据集样本 ID，由后端解析：

```text
sample_id
  -> GT config path
  -> chart_type
  -> original image
  -> grid_with_grid image
  -> GT ticks/pixels
  -> GT objects/data
```

`grid_with_grid` 的来源优先级为：

1. GT config 显式给出的 `image_paths.grid_with_grid`。
2. GT config 显式给出的 `image_paths.with_grid`。
3. 已有 `encrypted_grid_path`。
4. `gt_grid_renderer.py` 根据 GT JSON 的 tick-pixel 参数绘制 fallback 图。

fallback 图仅表达 GT 网格坐标系，不表达任何模型预测。它使用灰色 `#cccccc`、`2px/2px` 虚线、`1px` 线宽，并且只给加密 tick 补 label。

### 13.2 生成端不泄漏

当前实验版遵守以下生成端约束：

- grid 阶段可使用 GT grid 图和 tick-pixel 映射，但不能直接注入 GT 答案。
- feedback 阶段只能基于 grid 或上一轮 feedback 的模型预测生成反馈图/反馈文本。
- feedback 提前停止依据是“当前预测与 grid 预测一致”或“当前 feedback 与上一轮 feedback 一致”，不是 GT/RNE。
- amplifier crop 中心来自上一轮模型预测或非 GT 几何先验；不能在预测缺失时回退 GT 数值。
- GT 只在阶段结束后用于 `RE`、`RNE`、`gt_metrics.json` 和日志复盘。

### 13.3 三机制覆盖

当前每个实验类型都声明在：

```text
backend/evaluation_prediction/common/experiment_contract.py
```

统一机制为：

```text
baseline: 1 次，论文对照组
grid: 1 次，full flow 第一阶段
feedback: 1-2 次，预测一致则提前停止
amplifier: 1-3 次，预测一致则提前停止；目标不可见返回 readable=false 并计入本轮，下一轮扩大 ROI
```

当前 final selection 属于机制内部的“读数汇总”，不新增模型调用，也不允许读取 GT：

- bar/line 类：优先使用 amplifier 阶段的可读序列；如果 grid 与 feedback 已经稳定，而 amplifier 序列中位数相对稳定预测偏离超过数值轴跨度约 `6%`，则判为预测间离群并回退到稳定的 grid/feedback。该规则用于避免局部 crop 读错刻度区间，同时仍只比较模型预测之间的距离。
- scatter/bubble 类：full-flow final 在 grid、feedback、feedback_crop_adaptive/amplifier 的可读 XY 预测中选择；点大小辅助只作为 amplifier crop 尺度和提示上下文，不单独发起模型请求。
- pie/donut 类：percentage 是主读数，start/end angle 是 feedback 绘图和 amplifier 裁剪的辅助读数。final 只来自真实 grid/feedback/amplifier 调用，不再使用 `single_missing_remainder`、`amplifier_pct` 或 baseline fallback。
- radar/rose 类：径向值 `r` 是主读数，polar amplifier 使用局部红色径向/圆环 guide；当前 coverage 审计中 polar grid 有重复计数现象，需要继续清理统计口径。

三机制覆盖不是只靠文档约定，而是由完整性检查脚本审查：

```text
backend/evaluation_prediction/gt_experiment_flow_integrity_check.py
```

该脚本对 9 类 synthetic 样本逐类检查 GT loader、目标枚举、GT grid 图、prompt 生成、feedback 图、amplifier crop、轮次配置、GT 泄漏风险、stacked bar 排除和 `call_id` 关联。

最近一次执行：

```powershell
python backend/evaluation_prediction/gt_experiment_flow_integrity_check.py --source synthetic
```

结果：

```text
bubble: PASS
scatter: PASS
line: PASS
v_bar: PASS
h_bar: PASS
pie: PASS
donut: PASS
radar: PASS
rose: PASS
```

### 13.4 日志与指标闭环

每次 GT 实验运行都会创建：

```text
gt_runs/<chart_type>/<chart_name>/<run_id>/
```

该目录同时保存：

- `gt_config.json`：本次使用的 GT config 快照。
- `gt_metrics.json`：结构化 GT、预测值、RE/RNE。
- `gt_metric_records.csv`：论文统计用扁平指标表。
- `stage_coverage.json`：逐对象阶段覆盖审计。
- `modal_calls.jsonl`：每次模型调用摘要日志。
- `modal_calls_full.jsonl`：每次模型调用完整输入输出。
- `modal_calls_enriched.jsonl`：通过 `call_id` 补入结构化预测的摘要日志。
- `modal_calls_full_enriched.jsonl`：通过 `call_id` 补入结构化预测的全量日志。
- `modal_call_records.csv`：每次 modal call 的人工可读扁平表，包含图表名称、处理对象、对象类别、GT、按类型拆分后的预测值、attempts、输入 token、输出 token、文字提示词 token、单轮请求耗时和 RE/RNE。
- `pf/`：各 runner 的过程 CSV、overlay、crop 和可视化图。

`call_id` 是当前日志链路的主键。它由 `gemini_calls.py` 在发起 modal call 时生成，runner 在解析模型输出后读取同一个 `call_id` 写入结构化预测记录。后端汇总时再用 `call_id` enrich 原始日志，因此模型原始输入输出、runner CSV、GT 指标表可以显式关联。

### 13.5 真实模型验证

已经真实接入模型完整运行过一个 synthetic `h_bar`：

```text
sample_id = 661ec486776ed0d0
chart = h_bar_001
run_dir = gt_runs/h_bar/h_bar_001/g25fnt__h_bar_001
```

该 run 的关键结果：

```text
chart_runs = 1
record_count = 16
missing_stage_object_count = 0
missing_valid_full_flow_object_count = 0
stage_call_violation_object_count = 0
modal_calls.jsonl = 16 条
结构化预测记录 = 16 条
modal call 与结构化预测 call_id 缺失 = 0 条
baseline_avg_mae = 0.0033333333
baseline_avg_re = 0.0139
final_full_flow_avg_mae = 0.002
final_full_flow_avg_re = 0.0080333333
```

3 个 h_bar 处理对象都覆盖了 baseline、grid、feedback、amplifier。16 条 modal call 全部对应 16 条结构化预测记录，没有额外 `amplifier_exists` / crop 检测辅助调用；每条记录都有唯一 `call_id`，并且都能在 modal call 原始日志、runner CSV、`gt_metric_records.csv` 和 enriched 日志之间互相找到。当前 full-flow selected predictions 全部来自 amplifier：Vanuatu=0.512、Sri Lanka=0.237、Timor=0.131；三者平均 MAE/RE 均优于 baseline。这个真实 run 证明输出目录、过程文件、全日志、结构化指标和 `call_id` 关联链路已经能在真实模型调用下闭合。

### 13.6 当前可认为完成的目标

| 目标 | 当前状态 |
|---|---|
| 基于 GT 类型、GT grid、GT tick-pixel 映射进行评估预测 | 完成 |
| baseline 与 full flow 都输出 RE/RNE | 完成 |
| grid 1 次、feedback 最多 2 次、amplifier 最多 3 次 | 完成 |
| 提前停止不使用 GT/RNE | 完成 |
| amplifier 不用 GT 作为 crop 中心 | 完成 |
| 每个图表按类别分层输出并保存过程文件 | 完成 |
| 每次 modal call 记录输入输出、token、attempts、耗时 | 完成 |
| modal raw log 与结构化预测通过 `call_id` 显式关联 | 完成 |
| 9 类图表三机制完整性检查 | 完成 |
| h_bar 真实模型端到端运行验证 | 完成 |

### 13.7 仍需保持警惕的维护点

- 如果后续修改 prompt，必须确认没有把 GT 答案值直接写进模型提示词。
- 如果后续修改 feedback/amplifier 视觉生成，必须确认图中只画模型预测，不画 GT 真值。
- 如果后续修改 GT grid fallback renderer，必须重新检查灰色 GT grid 样式和“只给加密对象 label”的规则。
- 如果后续新增类别，必须先补 `experiment_contract.py`、runner、prompt、visual、model 和完整性检查，再纳入论文实验。
- 如果后续批量跑真实模型，建议增加批量汇总报告，按 chart/type/object/stage 聚合 RE/RNE、token 和耗时。
