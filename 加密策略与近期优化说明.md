# 网格加密策略与近期优化说明

本文档说明当前系统的网格还原、加密、缓存和评估预测策略。当前口径：直角坐标系以本项目 `main` 为准；`origin/stacked_bar` 分支只参考极坐标系处理，不作为直角系实现来源。

## 1. 总体链路

```text
上传图片
  -> MLLM 类型识别和结构先验
  -> 按图表类型分发
  -> 网格/坐标轴/tick/label/颜色识别
  -> 生成基础网格和加密网格
  -> 写入系统 JSON
  -> 评估预测读取系统 JSON 和网格图
```

生成端不得读取数据集 GT。GT 只出现在离线评估端，用于计算指标。

## 2. 直角坐标系网格还原

直角系包括：

- `v_bar`
- `h_bar`
- `line`
- `scatter`
- `bubble`

核心实现位于：

```text
backend/Grid_generation/grid_generation.py
backend/Grid_generation/grid_ocr.py
backend/Grid_generation/grid_line_filter.py
backend/Grid_generation/grid_adjudication.py
```

处理依据：

- 图像中的坐标轴、背景网格、tick 短线、数据标记等 CV 证据。
- OCR/MLLM 识别出的可见 tick label 和 label 框。
- 上传阶段 MLLM 提供的保守结构先验。
- 与 `F:\program\grid` 已验证机制对齐的网格恢复逻辑。

## 3. 加密 tick 策略

只对数值轴加密：

```text
linear: (tick_i + tick_{i+1}) / 2
log:    sqrt(tick_i * tick_{i+1})
```

文字轴、日期轴、类别轴不生成中间 label。

密集图保护：

- 计算加密后相邻 tick 的像素间距。
- 读取 OCR label 框的宽度或高度。
- 如果 label 框放置空间不足，则该数值轴整体不加密。
- 不做“部分区间加密、部分区间不加密”，避免轴语义不一致。

## 4. 网格绘制样式

标准灰色主流程：

- 颜色：`#cccccc`
- RGB：`(204, 204, 204)`
- 线宽：`1px`
- 虚线：`(0, (2, 2))`，即 2px 实线 + 2px 空白循环
- 原生识别网格线和加密网格线都使用该样式

彩色预览：

- 用于人工检查候选线、偏移和加密效果。
- 前端可切换显示。
- 不作为正式主流程产物。

新生成 label：

- 文本使用黑色。
- 两个轴都使用黑色。
- 位置和朝向参考 OCR label 框和原轴 label 布局。

## 5. 缓存策略

缓存分为两类：

| 缓存 | 用途 |
| --- | --- |
| 模型缓存 | 避免同一图片、同一 prompt、同一模型重复调用 MLLM |
| 数据集预览缓存 | 让前端快速展示数据集样例的网格和预测结果 |

自定义上传不读取数据集预览缓存。数据集预览缓存必须来自本项目真实处理链路，不能来自 reference，也不能用 GT 生成结果。

当前 Gemini 默认模型为：

```text
gemini-2.5-flash-lite
```

## 6. 图表类型边界

当前主流程支持 9 类：

```text
rose, radar, v_bar, h_bar, line, scatter, bubble, donut, pie
```

`stacked_bar` 分支不再作为直角系类型来源。该分支中值得参考的是极坐标系代码整理、评估和 fallback 组织方式。

## 7. 评估指标

全量评估报告中主要关注：

- 数值轴 tick-value MAE。
- 数值轴 tick-value Acc@2px。
- 图例颜色准确率，可配置 RGB 距离阈值。
- 标签名准确率。
- 图表分类准确率。

tick MAE 和 tick Acc 只计算数值轴，分类轴不纳入这两个指标。

## 8. 近期维护结论

- 类型识别失败应直接报错，不再给默认错误类别。
- 数据集 GT 分类应从明确分类目录得到，不应依赖 `ALL` 混合目录。
- 自定义上传与数据集预览的结果若不一致，应优先检查缓存是否由当前链路重新生成。
- 直角系不要合入 `stacked_bar` 分支的实现变化。
- 极坐标系后续可以吸收 `stacked_bar` 分支中的模块化处理，但仍需遵守“不用 GT 生成”的边界。
