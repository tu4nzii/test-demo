# 加密策略与近期优化说明

本文档说明当前系统的图表网格加密策略，并对比最近一次 Git 提交
`32d474b Merge branch 'stacked_bar'` 之后的主要优化点。

这里的“加密”指在原图上生成辅助网格和插值 tick，让模型在后续评估预测时能更稳定地读取图表对象的数值。系统上传处理的主流程不依赖数据集 GT JSON；GT 只应出现在后门或离线评估场景中。

## 一、整体处理链路

当前系统的直角系加密链路可以概括为：

1. 上传图像。
2. MLLM 识别图表类型，并同步输出轴/tick 先验。
3. 后端按图表类型分发到对应处理模块。
4. CV 检测坐标轴、tick、柱体、点、背景网格等视觉结构。
5. MLLM 读取可见 tick 标签、轴角色、部分图表的条形数据标签。
6. 将 tick 文本与像素位置绑定，并用网格、轴线、数据对象做校验和修正。
7. 只对数值轴插入中间加密 tick；文字轴不插值。
8. 生成基础网格图、加密网格图和系统生成 JSON。
9. 评估预测阶段使用系统生成的 JSON 和加密图，不使用数据集自带 GT JSON。

主入口关系如下：

- 上传识别：`backend/type_detection/chart_type.py`
- 统一处理入口：`backend/main.py`
- 直角系网格生成：`backend/Grid_generation/grid_generation.py`
- tick 文本读取：`backend/Grid_generation/function_calling/label/extract_tick_labels_with_llm.py`
- tick 短线扫描：`backend/Grid_generation/function_calling/ticks/detect_ticks.py`
- 批量真实图复查：`backend/evaluation/scripts/run_realworld_grid_only.py`

## 二、上传阶段的 MLLM 先验

上传阶段不再只识别图表类型，而是同步读取一组用于指导后续 CV 流程的图表属性：

- `type`：规范化后的图表类型，如 `v_bar`、`h_bar`、`scatter`、`bubble`、`line`、`v_stacked_bar`。
- `axis_repair`：是否缺轴、缺 tick、是否需要从背景网格恢复 tick。
- `x_axis_role` / `y_axis_role`：数值轴、类别轴、日期轴或未知。
- `x_axis_position` / `y_axis_position`：轴在底部、顶部、左侧、右侧、中间等。
- `plot_area_style`：显式轴、弱轴、仅网格、无轴。
- `has_background_grid`：是否存在可用于恢复 tick 的背景网格。
- `bar_layout` / `bar_orientation`：单柱、分组、堆叠、密集，以及水平/垂直诊断信息。
- `axis_tick_labels`：上传阶段保守读取到的可见 tick 文本。

这个先验是“保守启用”的：如果图表本身轴和 tick 完整，修复标志保持 false，后续走原有 CV 逻辑；只有 MLLM 明确判断缺轴、弱轴、仅网格，或 CV 确实找不到轴/tick 时，才进入补轴、补 tick 或网格恢复逻辑。

这样做的目的不是让 MLLM 替代 CV，而是让 MLLM 决定采用哪类 CV 原子操作。例如：

- 右侧 Y 轴：后续生成 v_bar 轴线时把 Y 轴放到右侧。
- 背景网格可靠：允许从浅灰色网格线恢复 tick 像素。
- 柱形图缺数值轴 tick：允许读取柱端数据标签或合成数值轴 tick。
- scatter/bubble 轴很弱：优先从背景网格推断 plot bounds。

## 三、直角系基础加密策略

### 1. 坐标轴与 tick 获取

系统首先通过 Hough 线段、黑色像素短线扫描、柱体/点的视觉包围盒推断坐标轴和 tick 像素。随后调用 MLLM 读取 tick 标签。

tick 读取 prompt 已按图表类型细化：

- scatter/bubble：只读取主绘图区坐标轴，忽略色条、尺寸图例、系列图例、注释和说明文字。
- line：忽略数据点旁的数值标注、曲线标注、右侧系列标签等。
- h_bar：Y 轴按从上到下读取类别标签，避免把底部数值轴当成 Y 轴。
- bar/grouped bar：多级类别轴只读与柱体直接对齐的主类别标签，忽略年份、分组标题、图例和轴标题。

### 2. 数值轴和文字轴分离

当前加密只对数值轴插入加密 tick：

- 数值轴：在相邻原始 tick 之间插入一个中间值。
- 文字轴：保持原类别 tick，不插入中间类别。

这避免了把类别轴、月份、国家名、条目名错误插值成无意义的“加密标签”。

### 3. 加密 tick 的计算

对线性数值轴：

```text
encrypted_tick = (tick_i + tick_{i+1}) / 2
```

对对数数值轴：

```text
encrypted_tick = sqrt(tick_i * tick_{i+1})
```

系统会根据 tick 值和像素位置判断轴尺度是 `linear` 还是 `log`。例如 x 轴为 2000、5000、10000、20000、50000、100000 这类非等差但近似等比的序列时，会优先尝试对数映射。

### 4. 加密像素的计算

加密 tick 对应的像素位置不是简单按列表硬塞，而是基于数值和轴尺度插值：

- 线性轴：按数值比例在线段上插值。
- 对数轴：先对 tick 取 `log10`，再在线段上插值。
- Y 轴：保持图像坐标方向，即数值越大通常像素 y 越小。

原始 tick 像素和插入 tick 像素组成最终的 `x_pixels_encrypted`、`y_pixels_encrypted`。

### 5. 网格绘制样式

第二步加密处理中的网格样式为：

- 颜色：`#cccccc`
- 线宽：`1px`
- 虚线：`2px 实线 + 2px 空白` 循环
- 抗锯齿：OpenCV `LINE_AA`

基础网格图绘制原始 tick 的网格线；加密网格图只额外绘制插入的中间 tick 网格线。这样可以让加密信息清晰可见，同时不重复强化原始网格。

测试复查脚本可以通过环境变量 `CHART_GRID_REVIEW_COLOR=green` 把网格临时染成绿色，便于人工检查偏移问题。正式系统不需要设置这个变量，因此不会改变用户正常看到的灰色加密效果。

### 6. 加密文本标签

加密 tick 标签只绘制插入的中间 tick：

- 文本颜色：红色。
- 文本背景：半透明白底，降低对图表内容的遮挡。
- 对小数位做统一格式化，避免浮点误差暴露。
- Y 轴如果在右侧，标签优先绘制在右侧；空间不足时回退到内侧。

## 四、图表类型专用策略

### 1. v_bar / h_bar / stacked bar

bar 系列的核心原则是区分类别轴和数值轴：

- `v_bar` / `v_stacked_bar`：X 轴通常为文字类别轴，Y 轴为数值轴。
- `h_bar` / `h_stacked_bar`：Y 轴通常为文字类别轴，X 轴为数值轴。

当前策略包括：

- 用柱体包围盒推断缺失轴线。
- 对 grouped bar，把多个柱体中心聚类成类别中心，避免把同一组内多个柱子当成多个类别 tick。
- 对右侧 Y 轴图表，按 MLLM 的 `y_axis_position=right` 把数值轴放到右侧。
- 如果数值轴 tick 丢失，但柱端有数值标签，则调用 MLLM 读取柱端数据标签，再由柱体长度/高度反推一组“漂亮刻度”。
- 对数值轴候选 tick 过多的情况，按 MLLM 读取到的 tick 数量进行绑定，避免密集短线、边框、网格线被误当成 tick。
- 对缺 tick 且无可靠标签的柱形图，才使用有限的合成 tick 兜底，避免直接失败。

这套策略尤其用于处理真实图里常见的问题：缺轴、右侧轴、分组类别轴、多级标签、柱端数据标签替代坐标轴 tick。

### 2. scatter / bubble

scatter 和 bubble 的主要难点是：真实图经常有弱轴、无短 tick、浅灰背景网格、色条/大小图例、局部网格线等干扰。

当前策略包括：

- 从浅灰色背景网格中投影检测候选竖线和横线。
- 对候选线组合做数值绑定：
  - 枚举候选像素组合。
  - 分别尝试线性映射和对数映射。
  - 计算拟合误差，选择误差更小且覆盖范围合理的组合。
- 在 tick 像素只覆盖局部绘图区时，用背景网格或点的视觉边界扩展 plot bounds。
- 对轻微偏移的 tick，允许 snap 到附近真实网格线，但设置较小容差，避免大幅改变原数据集中的正常图。
- 对 0 到 1 这类归一化前缀轴，结合点云包围盒补齐完整映射。
- 对色条、图例、说明文字等干扰，在 MLLM prompt 和 CV 绑定阶段都做排除。

这部分让 BubbleChart14、BubbleChart15 这类“双轴缺失且背景网格不均匀”的真实图，不再单纯依赖默认轴位置，而是从真实网格结构恢复映射。

### 3. line

line 图的策略更接近 scatter，但对 prompt 做了额外约束：

- 不把数据点旁边的数字标注当 tick。
- 不把曲线右侧的系列标签当 Y 轴 tick。
- 对日期、月份、年份等 X 轴文本，优先保持文字轴语义，不强行数值化。
- 如果数值轴短线候选过多，同样用 MLLM 读取到的 tick 数量和轴线范围进行约束。

### 4. pie / donut

pie 和 donut 不是直角坐标系，使用角度网格策略：

- 生成角度辅助线，作为 `with_grid` 图。
- 系统生成 JSON 中保留 `image_paths.with_grid`。
- 评估预测链路应使用系统处理过程生成的 JSON 和网格图，而不是数据集自带 JSON。
- donut 的预测链路已经从单纯整图读取升级为“整图初提取 + 每个标签 grid/feedback/amplifier 精修”的方向，避免整图结果直接替代精修阶段。

### 5. radar / rose

radar 和 rose 属于极坐标类：

- 重点先定位圆心或多边形中心。
- 对圆形雷达图和多边形雷达图采用不同检测策略，避免为了真实图多边形适配而破坏原数据集圆形雷达图效果。
- 当刻度文本不可辨识时，可以模拟径向映射关系，但不能从 GT JSON 补信息。
- 评估预测阶段使用系统过程产物，而不是数据集 GT。

## 五、系统 JSON 产物

处理完成后，系统会把 tick、像素、轴类型、图像路径写入系统生成 JSON。关键字段包括：

- `x_ticks` / `y_ticks`
- `x_pixels` / `y_pixels`
- `x_ticks_encrypted` / `y_ticks_encrypted`
- `x_pixels_encrypted` / `y_pixels_encrypted`
- `x_axis_type` / `y_axis_type`
- `x_axis_scale` / `y_axis_scale`
- `basic_grid_path`
- `encrypted_grid_path`
- `image_paths.no_grid`
- `image_paths.with_grid`
- `image_paths.grid_with_grid`

这个 JSON 是后续评估预测的输入来源。正常用户上传流程不应读取 `backend/charts` 中的数据集 GT JSON。

## 六、相对最近一次提交的主要优化点

最近一次提交是：

```text
32d474b Merge branch 'stacked_bar'
```

当前工作区相对该提交，围绕加密链路的核心优化如下。

### 1. 上传阶段从“类型识别”升级为“类型 + 轴结构先验”

之前上传阶段主要返回图表类型。现在 MLLM 同步返回缺轴、缺 tick、轴角色、轴位置、plot 样式、背景网格、bar layout、上传阶段 tick 标签等信息。

优化价值：

- 真实图适配从写死特例变成工作流编排。
- 后续 CV 能按不同图表属性选择不同原子操作。
- 正常图表因为 `axis_repair` 默认关闭，不会被补轴逻辑干扰。

### 2. MLLM JSON 稳定性增强

类型识别请求增加：

- `max_tokens=4096`
- `temperature=0`
- `response_format={"type": "json_object"}`
- 更鲁棒的 JSON 解析，支持 markdown code fence、字段别名和 raw JSON 提取。

优化价值：

- 解决 Gemini 2.5 输出较长导致截断后误判为默认类型的问题。
- 避免因字段名不同导致 `type` 或 `confidence` 解析失败。

### 3. Gemini 默认模型切换为 `gemini-2.5-flash`

统一配置中的 Gemini 默认模型已从：

```text
gemini-3.1-flash-lite
```

切换为：

```text
gemini-2.5-flash
```

优化价值：

- 上传识别和 tick 读取更稳定。
- 真实图中复杂轴结构、缺轴、右侧轴、背景网格的判断更可靠。
- 仍然通过统一配置入口管理，后续可继续切换模型。

### 4. 缺轴和弱轴修复从“简单补线”升级为“提示驱动 + CV 校验”

现在补轴不再只根据某一条经验规则，而是结合：

- MLLM 的 `axis_repair`。
- CV 是否实际检测到轴线。
- 柱体/点/背景网格的视觉边界。
- 轴位置先验，如右侧 Y 轴。
- plot_area_style 是否为 `weak_axes` 或 `grid_only`。

优化价值：

- 对缺 Y 轴、缺 X 轴、右侧 Y 轴、弱轴图表更稳。
- 对正常图表保持原逻辑，减少误修复。

### 5. tick 绑定增加数值一致性校验

之前 tick 像素和 MLLM 读取值更容易出现简单顺序绑定。现在对候选网格线做：

- 候选线去重。
- 按 tick 数量选择组合。
- 线性拟合。
- 对数拟合。
- RMSE/拟合误差评分。
- endpoint 覆盖范围约束。
- 小偏移 snap 到附近视觉网格线。

优化价值：

- 解决 tick 整体错一位、轴端点没识别导致加密值错配的问题。
- 支持对数轴，如真实 bubble 图中 x 轴为非等距数值序列。
- 避免局部网格线、文字、图例线条被误用为 tick。

### 6. scatter/bubble 背景网格恢复增强

新增从浅灰色网格中恢复 plot bounds 和 tick 位置的逻辑：

- 对浅灰网格做像素投影。
- 分别恢复竖向/横向候选线。
- 从 Hough 线、投影峰值、灰色虚线 span 中组合候选。
- 结合 MLLM tick 值反选正确线组。
- 如果当前 tick 覆盖范围过小，用绘图区边界重新扩展轴范围。

优化价值：

- 改善双轴缺失、短 tick 不明显、背景网格比轴线更可靠的真实 scatter/bubble。
- 降低色条、图例、注释对主坐标轴的误导。

### 7. bar 系列增加分组、右侧轴、数据标签兜底

bar 系列新增或强化：

- grouped bar 类别中心聚类。
- 右侧 Y 轴位置支持。
- 根据柱体边界修复 v_bar 数值轴位置。
- 当数值轴 tick 缺失时，读取柱端数据标签并生成 value-axis tick。
- 对 bar 数值轴候选 tick 过多时，按 MLLM tick 数量绑定。

优化价值：

- 处理真实 grouped bar 的多级类别标签。
- 处理右侧轴图表。
- 处理无坐标轴 tick 但有柱端数字的图表。
- 避免把分组标题、年份、图例误读成类别 tick。

### 8. 去掉 bar 类型错误回退，避免结果落错目录

`backend/main.py` 中删除了处理失败后自动尝试另一种 bar 类型的 fallback。

优化价值：

- 避免识别为 `h_bar` 的图被回退处理后存到 `v_bar` 目录。
- 错误暴露更明确，方便定位真正的轴/tick 问题。

### 9. 测试脚本支持真实图全流程复查产物

`run_realworld_grid_only.py` 增强了：

- 路径安全短名。
- sidecar 产物复制。
- source/upload/with_grid 三类图保存。
- contact sheet 自动生成。
- 测试用绿色网格。
- manifest 和 summary 汇总。

优化价值：

- 可以批量跑真实图并保留中间产物。
- 方便人工对比原图和加密图。
- 不污染正式加密样式。

## 七、近期实验结果

使用 `gemini-2.5-flash` 重新跑直角系真实图和原数据集抽样：

| 数据集 | 成功数 | 失败数 | 输出目录 |
| --- | ---: | ---: | --- |
| 真实直角系主目录 `bar,bubble,line,scatter` | 71 | 0 | `backend/evaluation/recheck_outputs/realworld_cartesian_gemini25_primary_v2_20260610` |
| 真实直角系额外目录 | 100 | 0 | `backend/evaluation/recheck_outputs/realworld_cartesian_gemini25_extra_20260610` |
| 原数据集直角系抽样 | 35 | 0 | `backend/evaluation/recheck_outputs/original_dataset_cartesian_gemini25_sample_20260610` |

最终 JSON 结构检查结果：

- 主目录：142 个最终 JSON，tick/pixel 数量问题 0 个。
- 额外目录：200 个最终 JSON，tick/pixel 数量问题 0 个。
- 原数据集抽样：70 个最终 JSON，tick/pixel 数量问题 0 个。

这些结果说明当前优化没有只针对单张图写死，而是通过更丰富的上传先验和更细粒度的 CV 原子操作，提高了真实直角系图表的整体鲁棒性。

## 八、当前策略的边界

当前系统仍遵循几个保护原则：

1. 正常图表不主动补轴。只有 MLLM/CV 指出缺轴、弱轴、仅网格等情况才进入修复路径。
2. 文字轴不做数值加密。否则类别标签会产生无意义插值。
3. 上传阶段 tick 只作为先验。只有当前 tick 识别明显不可靠时才覆盖。
4. 真实图测试可以用绿色网格，但正式加密仍是灰色虚线。
5. 系统上传处理不读取数据集 GT JSON。

后续如果继续优化，应优先补充新的“图表属性判断 + CV 原子操作”，而不是为单张真实图写死坐标。

