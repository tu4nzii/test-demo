# 图表智能分析与加密处理项目文档

本文档描述当前项目的业务目标、系统架构、核心流程、数据边界、运行方式和维护注意事项。文档以当前代码实现为准，尤其明确两条关键约束：业务入口不应强制用户提供 JSON；图表加密处理必须基于原始图片识别结果，不得基于上传 JSON 的坐标或刻度字段生成加密网格。

## 1. 项目定位

本项目是一个图表智能分析 Demo 系统。用户在前端上传一张图表图片，后端完成图表类型识别、图表加密处理、结构化结果保存和评估结果生成，前端展示原图、加密后图片和评估摘要。JSON 数据只作为可选的评估/兼容输入存在，不是业务必填项。

项目的核心价值是把静态图表转换为带有加密网格/加密刻度的可检查图像，并提供一套围绕图表结构和数据点的评估接口。

## 2. 用户流程

1. 用户在前端选择图表图片，支持 PNG/JPEG。
2. 用户可选上传配套 JSON 数据文件；直角坐标系处理不需要 JSON。
3. 前端调用 `/api/upload/` 上传文件。
4. 后端保存文件，调用图表类型检测，返回 `chart_id`、`chart_type`、`coordinate_system` 和置信度。
5. 用户点击“加密处理”，前端调用 `/api/process/?chart_id=...`。
6. 后端根据图表类型选择对应处理器，生成加密图片并返回图片 URL。
7. 用户点击“进行预测”，前端调用 `/api/evaluate/?chart_id=...`。
8. 后端生成评估 JSON，前端读取并展示摘要和完整 JSON。

## 3. 核心需求

### 3.1 上传与识别

- 必须支持只上传图表图片完成业务主流程。
- JSON 文件是可选输入，仅用于评估或兼容历史/极坐标处理链路。
- 每次上传生成唯一 `chart_id`。
- 图表类型由后端自动识别，失败时使用默认类型继续流程。
- 上传后的 `chart_id` 存储在后端内存 `charts_db` 中，后端重启后会丢失。

### 3.2 加密处理

- 加密处理的输入是原始图片。
- JSON 不参与坐标轴、刻度位置、刻度值或加密刻度的生成。
- 直角坐标系图表基于图片检测坐标轴、刻度线，并通过图像/LLM 识别可见刻度标签。
- 数值轴只加密一轮：在每两个相邻可见刻度之间插入一个中间值。
- 文字轴不做数值加密，保持原始刻度。
- 生成结果包括基础网格图和加密网格图。

### 3.3 JSON 的职责边界

从业务角度看，系统不应要求用户必须提供 JSON。上传 JSON 的用途仅限于：

- 保存原始数据点，作为评估或结果富集的 ground truth。
- 写入后端生成的结果 JSON，供 `/api/evaluate/` 使用。
- 兼容部分历史流程或极坐标合作方代码的临时需求。
- 辅助前端/评估展示。

上传 JSON 不允许用于：

- 覆盖图片检测出的 tick 位置。
- 覆盖图片/LLM 识别出的 tick 值。
- 推导加密刻度或加密网格线。
- 在处理前复制成同名 companion JSON 供加密链路读取。

## 4. 支持的图表类型

图表类型集中注册在 `backend/type_detection/chart_registry.py`。

| 类型 | 名称 | 处理路径 |
| --- | --- | --- |
| `rose` | 玫瑰图 | `backend/demo_rose/` |
| `radar` | 雷达图 | `backend/demo_radar/` |
| `v_bar` | 垂直条形图 | `backend/Grid_generation/` |
| `h_bar` | 水平条形图 | `backend/Grid_generation/` |
| `line` | 折线图 | `backend/Grid_generation/` |
| `scatter` | 散点图 | `backend/Grid_generation/` |
| `bubble` | 气泡图 | `backend/Grid_generation/` |
| `donut` | 环形图 | 当前注册到通用处理器 |
| `pie` | 饼图 | 当前注册到通用处理器 |

注意：`pie`、`donut` 在语义上属于无直角坐标轴图表，但当前 registry 中与通用能力放在一起。若后续要正式支持它们，应补充专用处理器或修正坐标系分类。

## 5. 系统架构

### 5.1 后端

后端使用 FastAPI，入口为 `backend/main.py`。

主要模块：

| 模块 | 责任 |
| --- | --- |
| `backend/main.py` | API 路由、上传保存、流程编排、结果文件服务 |
| `backend/type_detection/chart_type.py` | 调用外部模型识别图表类型 |
| `backend/type_detection/chart_registry.py` | 图表类型、坐标系、能力注册表 |
| `backend/type_detection/chart_processor.py` | 处理器协议和工厂，根据图表类型路由 |
| `backend/Grid_generation/grid_generation.py` | 直角坐标系图表主处理流水线 |
| `backend/Grid_generation/function_calling/axis/` | 直线检测、合并、坐标轴推断 |
| `backend/Grid_generation/function_calling/ticks/` | 刻度线检测、过滤、位置修正 |
| `backend/Grid_generation/function_calling/label/` | 通过图像/LLM 识别轴刻度标签 |
| `backend/Grid_generation/function_calling/color/` | 通过图像/LLM 提取系列颜色 |
| `backend/demo_rose/` | 玫瑰图编码和轴查找 |
| `backend/demo_radar/` | 雷达图编码和轴查找 |
| `backend/evaluation/` | 评估数据归一化、指标计算、结果服务 |

### 5.2 前端

前端使用 Vue 3 + Vite，主要实现集中在 `frontend/chart-demo-ui/src/App.vue`。

主要职责：

- 管理图片文件、可选 JSON 文件和预览 URL。
- 调用上传、处理和评估接口。
- 展示图表 ID、类型、置信度。
- 展示加密后的图片。
- 展示评估摘要表和完整评估 JSON。

## 6. 直角坐标系加密流水线

直角坐标系图表由 `CartesianChartProcessor` 路由到 `Grid_generation.grid_generation.process_chart()`。

处理步骤：

1. 使用 OpenCV 读取原始图片。
2. 转灰度图。
3. 检测候选直线。
4. 合并相似直线。
5. 推断 X/Y 坐标轴。
6. 在坐标轴附近扫描刻度线。
7. 合并和过滤刻度线。
8. 使用 LLM 从图片中识别可见刻度标签和轴类型。
9. 使用 `refine_tick_pixels()` 对齐“刻度标签数量”和“检测出的像素位置”。
10. 数值轴调用 `generate_encrypted_ticks()` 插入中点。
11. 为插入的中点计算像素位置。
12. 绘制基础网格图 `_grid.png`。
13. 绘制加密网格图 `_with_grid.png`，只给新增加密刻度绘制红色标签。
14. 输出 `*_ticks.json` 保存检测和生成结果。

### 6.1 一轮加密规则

对数值轴，假设原图可见刻度为：

```text
56.0, 63.5, 71.0, 78.5, 86.0
```

一轮加密后应为：

```text
56.0, 59.75, 63.5, 67.25, 71.0, 74.75, 78.5, 82.25, 86.0
```

红色标签只显示新增项：

```text
59.75, 67.25, 74.75, 82.25
```

不能继续生成：

```text
57.875, 61.625, 65.375, ...
```

这些属于二次细分，不符合当前需求。

### 6.2 刻度位置修正规则

`refine_tick_pixels()` 的原则是：

- 如果图片/LLM 已识别出可见刻度标签数量，则像素位置必须收敛到同样数量。
- 检测到更多网格线或短线时，只能从中选择与可见标签数量匹配的位置，不能扩展为更多“原始刻度”。
- 只有在无法识别有效标签时，才允许使用检测到的位置做本地 fallback，以保持流程可运行。

## 7. API 说明

### 7.1 `GET /`

健康检查。

返回示例：

```json
{"message":"Chart Analysis API","version":"1.0.0"}
```

### 7.2 `POST /api/upload/`

上传图表图片，可选上传 JSON。

请求字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `file` | file | 图表图片 |
| `json_data` | file | 可选配套 JSON 数据 |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `chart_id` | 后续处理使用的唯一 ID |
| `chart_type` | 识别出的图表类型 |
| `coordinate_system` | 坐标系类型 |
| `confidence` | 类型识别置信度 |

### 7.3 `POST /api/process/`

执行加密处理。

查询参数：

| 参数 | 说明 |
| --- | --- |
| `chart_id` | 上传接口返回的 ID |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `encrypted_image_url` | 加密后图片路径 |

### 7.4 `POST /api/evaluate/`

生成评估结果。

查询参数：

| 参数 | 说明 |
| --- | --- |
| `chart_id` | 上传接口返回的 ID |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `results_url` | 评估 JSON 路径 |

### 7.5 `GET /api/images/{filename}`

返回上传图片或处理后图片文件。

### 7.6 `GET /api/results/{filename}`

返回评估结果 JSON。

## 8. 评估逻辑

评估入口为 `backend/evaluation/service.py` 的 `evaluate_chart_data()`。

评估数据来源：

- `ground_truth`、`data_points` 或 `data` 作为真实数据。
- `predictions`、`prediction`、`predicted_data`、`prediction_data`、`extracted_data`、`estimated_data` 作为预测数据。

如果存在预测数据，则计算：

- 匹配数量。
- 覆盖率。
- MAE。
- 相对误差。
- 额外预测项。

如果不存在预测数据，则返回 `data_readiness`，用于说明当前图表数据和结构是否具备评估条件。

评估还会检查结构质量：

- 是否存在 ground truth。
- 是否存在基础网格图。
- 是否存在加密网格图。
- X/Y 或极坐标 tick 数量。
- 颜色数量。

## 9. 运行目录和产物

运行时目录集中在 `backend/data/`。

| 路径 | 说明 |
| --- | --- |
| `backend/data/upload/` | 上传的图片和可选 JSON |
| `backend/data/output/{chart_type}/` | 处理后图片、中间 JSON |
| `backend/data/results/` | 评估结果 |
| `backend/data/llm_cache/` | LLM 识别缓存 |
| `backend/data/logs/` | 运行日志 |

直角坐标系处理成功后通常生成：

| 文件 | 说明 |
| --- | --- |
| `{image_stem}_grid.png` | 基础网格图 |
| `{image_stem}_with_grid.png` | 加密网格图 |
| `{image_stem}_ticks.json` | 刻度、像素位置、加密刻度、颜色等信息 |
| `{chart_id}_axes.json` | 轴信息 |
| `{image_stem}.json` | 供评估使用的聚合 JSON |

## 10. 本地启动

### 10.1 后端

```powershell
cd backend
python main.py
```

后端地址：

```text
http://127.0.0.1:8000
```

关闭占用：

```text
PS F:\program\test-demo\backend> netstat -ano | findstr ":8000"
  TCP    127.0.0.1:8000         0.0.0.0:0              LISTENING       15704
PS F:\program\test-demo\backend> taskkill /F /PID 15704
成功: 已终止 PID 为 15704 的进程。
```

### 10.2 前端

```powershell
cd frontend/chart-demo-ui
npm run dev
```

前端地址：

```text
http://127.0.0.1:5173
```

Windows PowerShell 如果拦截 `npm.ps1`，优先使用 `npm.cmd`。

## 11. 验证命令

后端语法检查：

```powershell
python -m py_compile backend/main.py
python -m py_compile backend/Grid_generation/grid_generation.py
python -m py_compile backend/Grid_generation/function_calling/ticks/refine_ticks.py
```

后端健康检查：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8000/
```

前端构建：

```powershell
cd frontend/chart-demo-ui
npm.cmd run build
```

## 12. 已知边界和注意事项

- 图表类型识别、刻度标签识别、颜色提取依赖外部 LLM API；网络或凭证异常时会影响结果。
- `charts_db` 是内存存储，后端重启后历史 `chart_id` 不可继续使用。
- 当前加密逻辑重点覆盖直角坐标系图表；玫瑰图和雷达图走各自 demo 处理器。
- 上传 JSON 不是业务必填项；它不能参与加密，但有 JSON 时可参与结果富集和评估。
- LLM 缓存使用图片内容 hash、prompt 签名、模型和 schema 等信息，prompt 或图片变化后会产生新缓存。
- 运行产物可再生成，通常不应作为核心源码提交。
- 项目中存在大量样例图表和历史实验文件，修改或清理前应确认是否为用户保留数据。

## 13. 后续维护建议

1. 将“图片可单独上传”和“加密不能读取上传 JSON”的约束加入自动化测试。
2. 为 `generate_encrypted_ticks()` 增加单元测试，覆盖数值轴一轮加密、文字轴不加密、浮点精度。
3. 为 `refine_tick_pixels()` 增加测试，确保可见刻度数量不会被扩展成网格线数量。
4. 梳理 `pie`、`donut` 的坐标系和处理器归属。
5. 将外部 LLM API key 从源码迁移到环境变量或安全配置。
6. 将 `charts_db` 替换为持久化存储，避免后端重启后状态丢失。
7. 增加端到端测试：上传样例图、处理、检查加密标签数量和具体值。

