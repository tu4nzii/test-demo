# 图表智能分析与网格加密项目文档

本文档描述当前 `main` 分支的真实系统边界、端到端流程、数据使用规则和维护注意事项。当前代码以 `main` 为准：直角坐标系处理继续使用本项目现有流程；`origin/stacked_bar` 分支只作为极坐标系处理思路的参考来源，不作为直角系实现依据。

## 1. 项目定位

本项目是一个图表智能分析 Demo 系统。用户可以上传图表图片，后端完成图表类型识别、网格还原、加密网格生成和评估预测，前端展示原图、标准灰色网格图、彩色预览图和预测结果。

系统也支持基于内置数据集快速预览效果。数据集预览通过缓存加速，用户可以按数据源和类别加载样例，避免一次性加载全部图像。

## 2. 核心原则

- 生成端只依赖图片和系统识别结果，不使用数据集 GT。
- 自定义上传必须从 0 到 1 走当前系统链路，不读取已有数据集缓存。
- 数据集预览可以读取本项目生成的缓存，用于快速展示，不代表上传链路可以跳过处理。
- 直角坐标系网格还原以当前 `main` 的 `backend/Grid_generation/` 为准，并对齐 `F:\program\grid` 已验证的网格还原机制。
- `origin/stacked_bar` 分支只参考其中极坐标系的整理方式；直角系不从该分支继承或覆盖。
- 模型失败或返回非法类型时应报错，不给错误类别兜底。
- 上传 JSON 或数据集自带 JSON 只允许用于评估端计算指标，不允许参与生成端。

## 3. 支持的图表类型

图表类型集中注册在 `backend/type_detection/chart_registry.py`。

| 类型 | 坐标系 | 当前处理入口 |
| --- | --- | --- |
| `v_bar` | 直角坐标系 | `backend/Grid_generation/` |
| `h_bar` | 直角坐标系 | `backend/Grid_generation/` |
| `line` | 直角坐标系 | `backend/Grid_generation/` |
| `scatter` | 直角坐标系 | `backend/Grid_generation/` |
| `bubble` | 直角坐标系 | `backend/Grid_generation/` |
| `pie` | 极坐标/角度网格 | `Grid_generation.circular_angle_grid` |
| `donut` | 极坐标/角度网格 | `Grid_generation.circular_angle_grid` |
| `radar` | 极坐标 | `backend/demo_radar/` |
| `rose` | 极坐标 | `backend/demo_rose/` |

当前注册表不包含 `v_stacked_bar` 或 `h_stacked_bar`。如果模型或数据路径中出现 stacked bar 相关名称，会按普通 `v_bar` / `h_bar` 处理，避免改变主线直角系类别集合。

## 4. 用户主流程

### 自定义上传

1. 前端上传图片到 `POST /api/upload/`。
2. 后端保存图片，调用 MLLM 识别图表类型、轴先验、tick 先验、图例/系列颜色等。
3. 如果模型调用失败、返回空值或返回不支持的类型，上传接口返回错误。
4. 用户点击“加密处理”，前端调用 `POST /api/process/?chart_id=...`。
5. 后端按图表类型进入对应处理器，生成基础网格图、标准灰色加密图、彩色预览图和系统 JSON。
6. 用户点击“评估预测”，前端调用 `POST /api/evaluate/?chart_id=...`。
7. 后端基于系统生成 JSON 和网格图执行评估预测，并返回可展示结果。

### 数据集预览

1. 前端调用 `/api/dataset-preview/categories/` 获取类别。
2. 前端按类别调用 `/api/dataset-preview/samples/` 懒加载样例。
3. 用户选择样例后调用 `/api/dataset-preview/select/`。
4. 如果存在当前版本缓存，则直接返回缓存图像和预测结果；缓存缺失时再走系统处理。

数据集源：

| source | 目录 |
| --- | --- |
| `realworld` | `backend/datasets/VisHintPrompt_datasets/Final-RealDataset` |
| `synthetic` | `backend/datasets/VisHintPrompt_datasets/Sy.Dataset` |

## 5. 直角坐标系处理链路

直角坐标系图表由 `CartesianChartProcessor` 路由到：

```text
backend/Grid_generation/grid_generation.py::process_chart()
```

主要步骤：

1. 读取原始图片。
2. MLLM 上传阶段提供保守的图表结构先验。
3. CV 检测坐标轴、背景网格、tick 候选、柱体/点/折线等视觉结构。
4. OCR/MLLM 读取可见 tick 文本、轴类型、图例/系列颜色。
5. 将 tick 文本和像素位置绑定，并用数值一致性、OCR 框、背景网格进行校验。
6. 只对数值轴生成一轮中间 tick；文字/类别轴不加密。
7. 密集图会根据加密后相邻 tick 间距与 OCR label 框尺寸判断是否跳过加密。
8. 标准灰色模式绘制 `#cccccc`、`1px`、`2px 实线 + 2px 空白` 的虚线网格。
9. 彩色模式只用于人工检查偏移，不接入主流程结果。
10. 输出 `*_grid.png`、`*_with_grid.png`、`*_with_grid_color.png`、`*_ticks.json` 和系统聚合 JSON。

数值轴加密规则：

```text
linear: encrypted_tick = (tick_i + tick_{i+1}) / 2
log:    encrypted_tick = sqrt(tick_i * tick_{i+1})
```

加密以“整条数值轴”为单位。若某数值轴需要加密，则该轴全部可加密区间一起加密；若 label 密度过高导致不适合加密，则该轴整体不加密。

## 6. 极坐标系处理边界

当前极坐标系包括 `radar`、`rose`、`pie`、`donut`。

- `radar` / `rose` 仍使用 `backend/demo_radar/`、`backend/demo_rose/` 现有链路。
- `pie` / `donut` 使用角度网格处理。
- 后续可以参考 `origin/stacked_bar` 分支中极坐标系的模块化、评估和 fallback 处理方式。
- 即使参考外部分支，也不能把数据集 GT 纳入生成端。

## 7. 评估预测

评估预测阶段是独立链路，入口在：

```text
backend/main.py::evaluate_processed_chart()
backend/evaluation_prediction/
backend/evaluation/
```

自定义上传时，评估预测基于当前处理阶段生成的系统 JSON 和网格图。数据集预览时，可以读取系统全链路预先生成的预测缓存。评估端可使用 GT 计算离线指标，但生成端和前端展示链路不能用 GT 生成结果。

常用离线指标：

- 数值轴 tick-value MAE（像素位置）。
- 数值轴 tick-value Acc@2px。
- 图例颜色准确率，可按 RGB 距离阈值调整容差。
- 标签名准确率。
- 图表分类准确率。

## 8. 模型和缓存

模型配置集中在：

```text
backend/evaluation_prediction/common/model_config.py
backend/model_api_config.py
model_api_config.py
```

当前默认 Gemini profile 使用 `gemini-2.5-flash-lite`。API key 不应写入提交文件，应通过本地 ignored secret 或环境变量提供。

缓存原则：

- 模型输入不变时应复用缓存。
- 自定义上传不使用数据集缓存。
- 数据集预览缓存必须来自本项目当前真实链路，而不是 reference 或 GT。
- 网格缓存和评估预测缓存是两类产物，可以独立更新。

## 9. 主要目录

| 路径 | 说明 |
| --- | --- |
| `backend/main.py` | FastAPI 入口和流程编排 |
| `backend/type_detection/` | 图表类型识别、注册表、处理器工厂 |
| `backend/Grid_generation/` | 当前直角坐标系网格还原与加密主流程 |
| `backend/evaluation_prediction/` | 评估预测模型调用与结果提取 |
| `backend/evaluation/` | 离线评估脚本、指标和报告 |
| `backend/datasets/VisHintPrompt_datasets/` | 当前系统数据集预览源 |
| `backend/data/` | 运行时上传、输出、缓存、结果 |
| `frontend/chart-demo-ui/` | Vue 前端 |
| `reference/` | 只作参考，不允许运行时代码依赖 |

## 10. 验证命令

```powershell
python -m py_compile backend/main.py backend/type_detection/chart_type.py backend/type_detection/chart_registry.py backend/Grid_generation/grid_generation.py
cd frontend/chart-demo-ui
npm.cmd run build
```

启动后可检查：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8000/
```

## 11. 维护注意事项

- 不要用 GT 修生成端。
- 不要恢复“类型失败默认 v_bar”的兜底。
- 不要让 `reference/` 进入运行时 import。
- 不要把 `origin/stacked_bar` 的直角系改动合入主线。
- 不要提交本地模型密钥。
- 修改缓存版本或 label 样式后，需要明确缓存失效规则。
- 运行产物、报告和数据集文件修改前先确认是否为用户保留内容。
