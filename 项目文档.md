# GT 网格评估实验版项目文档

本文档描述当前分支 `experiment/gt-grid-evaluation` 的项目边界、实验目标、端到端流程、输出目录和完整性审查结论。当前分支是论文评估实验版，不是发布版图表上传 Demo。

## 1. 项目定位

本实验版的目标是隔离“自动网格生成是否准确”对论文评估实验的干扰。系统直接读取人工标注或确认过的 GT 类型、GT grid 图像、GT tick-pixel 映射和 GT 数据，只评估第 3 步“评估预测”的模型能力。

因此，实验版关注的问题是：

- 给定正确图表类型，模型能否从原图得到 baseline 预测。
- 给定正确 `grid_with_grid` 加密网格图，模型能否更准确地读数。
- 给定上一轮模型预测，feedback 和 zoom-in/amplifier 能否自适应修正。
- 每一次模型调用能否完整复盘，包括输入、输出、token、耗时、重试和结构化预测。

## 2. 实验版边界

实验版主流程不使用：

- 自定义上传图表。
- 自动图表类型识别。
- 自动网格生成作为实验输入。
- 加密处理入口。
- dataset preview 的缓存复用机制。
- `v_stacked_bar` 和 `h_stacked_bar`。

实验版主流程使用：

- 数据集已有原图。
- GT config 中的图表类型、对象、ticks、pixels、数据真值。
- 已有 `grid_with_grid` 图；如果仓库中缺图，则根据 GT JSON 中的 tick-pixel 参数绘制 fallback GT 网格图。
- 真实模型调用执行 baseline 和 full flow。
- GT 只用于评估指标和日志复盘，不用于生成端裁剪中心、预测值或提前停止条件。

## 3. 支持图表类型

当前实验对象只包含 9 类：

| 类型 | 预测对象 | 主要预测量 |
| --- | --- | --- |
| `bubble` | 单个 bubble 中心 | `x`, `y` |
| `scatter` | 单个点中心 | `x`, `y` |
| `line` | 某 series 在某 x 类别处的折线点 | `y` |
| `v_bar` | 某根竖柱或对象 | `y` |
| `h_bar` | 某根横柱或对象 | `x` |
| `pie` | 某个扇区 | `share` / `start_angle` / `end_angle` |
| `donut` | 某个环形扇区 | `share` / `start_angle` / `end_angle` |
| `radar` | 某 series 在某角度轴上的点 | `r` |
| `rose` | 某扇区外边界 | `r` |

`v_stacked_bar` 和 `h_stacked_bar` 不属于本实验版处理对象。旧代码中若存在 stacked helper，只能视为未启用兼容代码，不能纳入论文实验统计。

## 4. 前端实验流程

前端入口仍在：

```text
frontend/chart-demo-ui/src/App.vue
```

实验版前端用于快速选择实验对象：

1. 选择数据源：`synthetic` 或 `realworld`。
2. 选择图表类别。
3. 选择单个样本，或全选当前类别样本。
4. 预览原图和 GT grid 图。
5. 运行 GT 实验。
6. 查看结果目录、指标摘要和过程文件。

上传、加密处理、缓存复用不是实验版主工作流。

## 5. 后端实验入口

GT 实验 API 在 `backend/main.py`：

| 接口 | 职责 |
| --- | --- |
| `GET /api/gt-experiment/categories/` | 列出实验支持类别 |
| `GET /api/gt-experiment/samples/` | 按来源和类别列出样本 |
| `GET /api/gt-experiment/image/{sample_id}/{image_type}` | 返回原图或 GT grid 图 |
| `POST /api/gt-experiment/select/` | 选择单个实验样本 |
| `POST /api/gt-experiment/run/` | 对单个样本真实运行模型实验 |

运行实验时，后端会设置：

```text
CHART_EXPERIMENT_MODE=gt
CHART_EXPERIMENT_PRESERVE_GT=1
CHART_FEEDBACK_ROUNDS=2
CHART_AMPLIFIER_ROUNDS=3
CHART_BAR_AMPLIFIER_ROUNDS=3
CHART_PREDICTION_CONSISTENCY_TOLERANCE=0.0025
GT_MODAL_CALL_LOG_PATH=<run_dir>/modal_calls.jsonl
GT_MODAL_FULL_LOG_PATH=<run_dir>/modal_calls_full.jsonl
EVALUATION_PREDICTION_RESULTS_ROOT=<run_dir>/process_files
```

预测调度入口：

```text
backend/evaluation_prediction/service.py
```

各类型 active runner：

| 类型 | runner |
| --- | --- |
| `bubble` | `backend/evaluation_prediction/chart_modules/bubble/runner.py` |
| `scatter` | `backend/evaluation_prediction/chart_modules/scatter/runner.py` |
| `line` | `backend/evaluation_prediction/chart_modules/line/runner.py` |
| `v_bar` | `backend/evaluation_prediction/chart_modules/v_bar/runner.py` |
| `h_bar` | `backend/evaluation_prediction/chart_modules/h_bar/runner.py` |
| `pie` | `backend/evaluation_prediction/chart_modules/pie/runner.py` |
| `donut` | `backend/evaluation_prediction/chart_modules/donut/runner.py` |
| `radar` | `backend/evaluation_prediction/chart_modules/radar/runner.py` -> `polar_value.py` |
| `rose` | `backend/evaluation_prediction/chart_modules/rose/runner.py` -> `polar_value.py` |

## 6. 三大机制

实验分两条对照：

- `baseline`：原图 + 极简提示，不使用 grid，是论文对照组。
- `full flow`：`grid -> feedback -> zoom-in/amplifier`。

`baseline` 必须和 full flow 解耦。baseline 的预测只进入自己的日志和 RE/RNE 指标，用来作为基线方法比较；不能进入 full flow 的预测历史、feedback overlay、amplifier crop 中心，也不能作为 full-flow selected prediction 的兜底结果。

### 6.1 Grid

`grid` 固定执行 1 次。输入是 `grid_with_grid` 加密网格图和必要的 tick-pixel 映射提示。其作用是让模型在正确网格坐标系下第一次读数。

约束：

- 可以使用 GT grid 图和 GT tick-pixel 映射。
- 不能把 GT 答案值写进提示词让模型照抄。
- 输出作为 feedback 和 amplifier 的初始模型预测。

### 6.2 Feedback

`feedback` 最多执行 2 次。输入是上一轮模型预测，以及基于上一轮预测生成的 overlay 或文字反馈。

提前停止只能基于模型预测之间的一致性：

- 第 1 轮 feedback 若与 grid 预测一致，可停止。
- 第 2 轮 feedback 若与第 1 轮 feedback 一致，可停止。

不能用 GT 或 RNE 作为生成端停止条件。

### 6.3 Zoom-in / Amplifier

`zoom-in` / `amplifier` 最多执行 3 次。输入是上一轮模型预测生成的局部 crop 和局部 guide。

约束：

- crop 中心来自模型预测或非 GT 几何先验。
- 不能用 GT 数值帮模型定位正确区域。
- 若上一轮没有有效预测，应记录失败或跳过，不能用 GT 补位。
- 存在性判断合并进 amplifier 本身，不再单独调用 `amplifier_exists`。
- 每轮 amplifier 的模型输出同时承担“目标是否可读”和“数值提取”：可读则返回预测值，不可读则按同一 JSON 契约返回 `readable=false`，预测值字段使用 `null`。
- 不可读的 `readable=false` 计入 amplifier 轮次；下一轮扩大 ROI，最大扩大到上一轮的 2 倍。
- `attempts` 只表示网络/API 重试；不可读后扩大 ROI 属于下一轮 amplifier，不属于 attempts。
- 对 bar 类局部 crop，amplifier 使用原图 `no_grid` 作为底图，再叠加红色局部虚线网格和底部 tick ruler，避免把 GT grid 的灰色加密 label 和红色局部 guide 混在同一张过程图里。
- 当前 GT 实验将预测一致性阈值收紧为 `CHART_PREDICTION_CONSISTENCY_TOLERANCE=0.0025`。该阈值只比较模型预测之间的一致性，不使用 GT/RNE。

## 7. 图像样式

实验版必须区分两类图像。

GT grid 图用于 grid 阶段和中列预览：

- 颜色：`#cccccc`，RGB `(204, 204, 204)`。
- 虚线：`2px` 实线 + `2px` 空白循环。
- 线宽：`1px`。
- 网格线覆盖整张图像画布。
- 只给加密得到的 tick-value 绘制 label。

feedback / amplifier 过程图用于模型修正：

- 保留红色预测标记和红色局部 guide。
- 不渲染成灰色 GT grid 样式。
- 不把 GT 真值位置画进过程图。

加密 tick 的识别规则：

```text
最终 tick 序列 = 原始 tick_0, 加密中点_0_1, 原始 tick_1, 加密中点_1_2, ...
```

因此两端和偶数位 tick 视为原始 tick，奇数位中点 tick 视为加密 tick。fallback GT grid renderer 只给加密 tick 绘制 label。

## 8. 输出目录

每次实验先按图表类别分层，再按图表单独建目录：

```text
backend/evaluation_prediction/results/gt_experiments/<chart_type>/<chart_name>/<run_id>/
```

这样全量测试时，`bubble`、`scatter`、`h_bar`、`v_bar` 等结果会进入各自类别目录，避免所有图表直接堆在 `gt_experiments` 根目录。

历史散落目录可用下面脚本整理；默认 dry-run，加 `--apply` 后才移动：

```powershell
python backend/evaluation_prediction/organize_gt_experiment_results.py
python backend/evaluation_prediction/organize_gt_experiment_results.py --apply
```

核心文件：

| 文件 | 含义 |
| --- | --- |
| `gt_config.json` | 本次实验使用的 GT config 快照 |
| `<run_id>_gt_experiment_result.json` | API 返回的完整结果 |
| `gt_metrics.json` | 结构化 GT、预测值、RE/RNE |
| `gt_metric_records.csv` | 论文统计用扁平指标表 |
| `stage_coverage.json` | 每个对象 baseline/grid/feedback/amplifier 覆盖情况 |
| `modal_calls.jsonl` | 每次模型调用摘要日志 |
| `modal_calls_full.jsonl` | 每次模型调用完整输入输出日志 |
| `modal_calls_enriched.jsonl` | 按 `call_id` 补入结构化预测的摘要日志 |
| `modal_calls_full_enriched.jsonl` | 按 `call_id` 补入结构化预测的全量日志 |
| `process_files/` | runner 的过程 CSV、overlay、crop、可视化图 |

`call_id` 是 modal call 原始日志和 runner 结构化预测之间的显式关联键。

## 8.1 实验 GT 数据集目录

外部人工标注 GT grid 数据已整理到：

```text
experiment_gt_dataset/organized/
├─ Final-RealDataset/
│  ├─ Bubble_GT/
│  ├─ Scatter_GT/
│  ├─ Line_GT/
│  ├─ vBar_GT/
│  ├─ hBar_GT/
│  ├─ Radar_GT/
│  └─ Rose_GT/
└─ Sy.Dataset/
   ├─ Radar_50_GT/
   └─ Rose_50_GT/
```

每个类别目录统一包含：

```text
chart/          原图 no_grid
gt_grid/        人工提供的标准 GT grid 图
chart_configs/ 归一化后的 GT JSON，image_paths.grid_with_grid 指向 gt_grid
```

后端新增两个实验数据源：

- `source=experiment_gt_real`
- `source=experiment_gt_synthetic`

因此可直接通过 `/api/gt-experiment/categories/?source=experiment_gt_real` 和 `/api/gt-experiment/samples/?source=experiment_gt_real&category=radar` 访问；这批数据的 grid 阶段优先使用 `gt_grid/` 中现成图像，不再触发 fallback 绘制。

## 9. 指标

实验版统一输出：

```text
RE  = abs(pred - gt) / max(abs(gt), eps)
RNE = abs(pred - gt) / axis_range
```

不同类型的主要指标维度：

- `scatter` / `bubble`：`x`、`y`。
- `line` / `v_bar`：主要数值轴 `y`。
- `h_bar`：主要数值轴 `x`。
- `pie` / `donut`：`share`，并保留角度预测。
- `radar` / `rose`：`r`。

## 10. 真实模型验证

已真实接入模型完整跑过 synthetic 九类 smoke。`h_bar_001` 的单例目录仍可作为输出结构参考：

```text
sample_id = 661ec486776ed0d0
chart = h_bar_001
run_dir = backend/evaluation_prediction/results/gt_experiments/h_bar/h_bar_001/gemini-2.5-flash-lite__h_bar_001
```

结果摘要：

- `chart_runs=1`
- `record_count=16`
- `missing_stage_object_count=0`
- `missing_valid_full_flow_object_count=0`
- `stage_call_violation_object_count=0`
- 3 个处理对象都覆盖 baseline、grid、feedback、amplifier。
- `modal_calls.jsonl` 16 条，和 16 条结构化预测记录一一对应。
- 当前没有 `amplifier_exists` / crop 检测辅助 modal call；crop 质量由后续 amplifier 预测和指标自然反映。
- 16 条结构化预测都有唯一 `call_id`，且都能在 modal call 日志、runner CSV、GT 指标表和 enriched 日志之间找到。
- 该次运行 baseline 平均 MAE/RE 为 `0.0033333333 / 0.0139`；最终 full-flow selected predictions 全部来自 amplifier，平均 MAE/RE 为 `0.002 / 0.0080333333`，优于 baseline。

本轮迁移后的九类 synthetic smoke 结果如下，所有 tested object 的 full flow final 在 RE 和 RNE 上均优于 baseline，且 stage call violation 均为 0：

| 类型样本 | baseline RE | final RE | baseline RNE | final RNE | violation |
|---|---:|---:|---:|---:|---:|
| `bubble_023` | 62.323793 | 0.235965 | 389.227384 | 0.049996 | 0 |
| `scatter_001` | 0.028367 | 0.021453 | 0.026964 | 0.021738 | 0 |
| `line_001` | 325000000000.008 | 130952380952.382 | 0.010500 | 0.003218 | 0 |
| `v_bar_002` | 0.003806 | 0.003285 | 0.003050 | 0.002610 | 0 |
| `h_bar_001` | 0.013889 | 0.008038 | 0.005556 | 0.003333 | 0 |
| `pie_001_no_grid` | 0.037500 | 0.002095 | 0.016667 | 0.000931 | 0 |
| `donut_000` | 0.509015 | 0.038742 | 0.045556 | 0.003122 | 0 |
| `radar_000` | 0.586515 | 0.037957 | 0.459375 | 0.021587 | 0 |
| `rose_000` | 0.132893 | 0.043464 | 0.186667 | 0.060304 | 0 |

这次真实运行证明输出目录、全日志、结构化指标、`call_id` 关联链路和九类三机制流程均已闭合。`line_001` 含 GT 为 0 的点，因此 RE 会被零值分母放大；论文对比应同时查看 RNE。

九类最新目录均包含 `full_flow_final_predictions.csv`、`full_flow_final_selection_audit.csv`、`modal_calls.jsonl`、`modal_calls_full.jsonl` 和 `modal_calls_enriched.jsonl`，因此最终预测、选择依据、原始模型输入输出和结构化指标可以相互复盘。

## 11. 第一性原理完整性审查

从实验可信性的第一性原理看，当前系统需要同时满足四件事：

1. 输入隔离：实验输入来自 GT 类型、GT grid、GT tick-pixel 和 GT 数据，不被自动网格生成误差污染。
2. 生成端不泄漏：GT 答案不能进入提示词、feedback overlay、amplifier crop 中心或提前停止条件。
3. 三机制覆盖：每个类型都必须具备 baseline、grid、feedback、amplifier，且轮次预算一致。
4. 可复盘：每次模型调用与结构化预测、指标和过程文件必须能一一追踪。

当前审查结论：

| 维度 | 结论 | 证据 |
| --- | --- | --- |
| 实验分支边界 | PASS | legacy upload/process/cache flow 在实验分支中被禁用或不作为主流程 |
| 支持类型集合 | PASS | 仅 9 类实验对象，不含 stacked bar |
| GT grid 输入 | PASS | 优先已有 `grid_with_grid`，缺失时由 GT tick-pixel fallback 绘制 |
| 三机制轮次 | PASS | baseline/grid 1 次，feedback 最多 2 次，amplifier 最多 3 次 |
| 生成端停止条件 | PASS | 使用预测一致性，不使用 GT/RNE |
| amplifier crop 中心 | PASS | 使用上一轮模型预测或中性几何先验，不回退 GT |
| 日志复盘 | PASS | modal call、full log、enriched log、CSV、JSON 通过 `call_id` 关联 |
| 九类静态完整性检查 | PASS | `gt_experiment_flow_integrity_check.py --source synthetic` 全部通过 |
| 真实模型闭环 | PASS | synthetic 九类 smoke 已真实跑通，final RE/RNE 均优于 baseline，stage violation 均为 0 |

## 12. 验证命令

静态完整性检查：

```powershell
python backend/evaluation_prediction/gt_experiment_flow_integrity_check.py --source synthetic
```

语法检查：

```powershell
python -m py_compile backend/main.py backend/evaluation_prediction/common/runtime.py backend/evaluation_prediction/common/experiment_flow.py
```

前后端预览：

```text
backend:  http://127.0.0.1:8000
frontend: http://127.0.0.1:5173
```

## 13. 维护注意事项

- 不要把 GT 答案写入模型提示词。
- 不要用 GT/RNE 判断 feedback 或 amplifier 是否提前停止。
- 不要用 GT 数值作为 amplifier crop 中心。
- 不要把 feedback/amplifier 的红色过程 guide 改成灰色 GT grid 样式。
- 不要把 GT grid fallback 的灰色样式改成红色过程样式。
- 不要把 `v_stacked_bar` / `h_stacked_bar` 纳入当前实验对象。
- 修改日志字段后必须确认 `call_id` 仍能关联 modal raw log、runner CSV、`gt_metric_records.csv` 和 enriched logs。
- 修改 GT grid renderer 后必须重新跑九类完整性检查。
