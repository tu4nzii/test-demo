# 图表智能分析系统使用指南

本指南面向本项目当前 `main` 分支。系统支持自定义上传和数据集快速预览两种入口，核心功能是图表类型识别、网格还原、网格加密和评估预测。

## 1. 启动服务

后端：

```powershell
cd F:\program\test-demo\backend
python main.py
```

默认地址：

```text
http://127.0.0.1:8000
```

前端：

```powershell
cd F:\program\test-demo\frontend\chart-demo-ui
npm.cmd install
npm.cmd run dev
```

默认地址：

```text
http://127.0.0.1:5173
```

PowerShell 如果拦截 `npm.ps1`，请使用 `npm.cmd`。

## 2. 自定义上传流程

1. 在前端选择一张图表图片。
2. 点击上传，系统调用 `/api/upload/`。
3. 上传成功后会显示 `chart_id`、`chart_type`、`coordinate_system` 和模型置信度。
4. 点击“加密处理”，系统调用 `/api/process/`。
5. 前端显示标准灰色网格图，并在可用时支持切换彩色预览图。
6. 点击“评估预测”，系统调用 `/api/evaluate/`。
7. 前端展示预测对象、数值、处理 JSON 和相关摘要。

自定义上传必须走当前系统链路，不读取数据集缓存，也不使用数据集 GT。

## 3. 数据集快速预览

前端支持从内置数据集中按类别加载样例：

- `Final-RealDataset`
- `Sy.Dataset`

为了避免加载过慢，前端按类别请求样例列表，不一次性加载全量数据。选择样例后，如果系统已有当前版本缓存，会直接展示缓存中的网格图和评估预测结果；如果缓存缺失，则按当前系统方法处理。

数据集预览缓存只用于快速展示，不能影响自定义上传。

## 4. 支持类型

| 类型 | 说明 | 坐标系 |
| --- | --- | --- |
| `v_bar` | 垂直柱状图 | 直角 |
| `h_bar` | 水平柱状图 | 直角 |
| `line` | 折线图 | 直角 |
| `scatter` | 散点图 | 直角 |
| `bubble` | 气泡图 | 直角 |
| `pie` | 饼图 | 极坐标/角度 |
| `donut` | 环形图 | 极坐标/角度 |
| `radar` | 雷达图 | 极坐标 |
| `rose` | 玫瑰图 | 极坐标 |

当前主流程不单独注册 stacked bar 类型。若路径或模型文本中出现 stacked bar，会按普通柱状图处理。

## 5. 图表类型识别

类型识别由 MLLM 完成，提示词要求模型只返回注册表支持的类型。上传阶段还会提取轴结构先验、可见 tick label、图例/系列颜色和 radar 网格形态等信息。

如果模型调用失败、返回空结果或返回不支持的类型，上传接口会返回错误。系统不会再用错误的默认类别继续处理。

## 6. 网格加密显示

直角系标准模式：

- 网格颜色：`#cccccc`
- 线宽：`1px`
- 虚线：`2px 实线 + 2px 空白`
- 新生成的 label：黑色文本
- 原生识别网格线和加密网格线在标准模式下使用同一灰色样式

彩色预览模式只用于人工检查线的位置和加密效果，不作为主流程输出。

加密规则：

- 只加密数值轴。
- 文字轴、类别轴不插入中间值。
- 对密集图，会比较加密后相邻 tick 间距和 OCR label 框尺寸；空间不足时整轴不加密。
- 一个数值轴要么整体加密，要么整体不加密。

## 7. 评估预测

“评估预测”使用系统生成的 JSON 和网格图作为输入。它不是 GT 生成器，也不应读取数据集自带 JSON 来构造前端展示结果。

数据集全量报告中可以使用 GT 计算指标，例如：

- 数值轴 Tick MAE。
- 数值轴 Tick Acc@2px。
- 图例颜色准确率。
- 标签名准确率。
- 图表分类准确率。

## 8. 常用接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/upload/` | POST | 上传自定义图片并识别类型 |
| `/api/process/` | POST | 对 `chart_id` 执行网格加密 |
| `/api/evaluate/` | POST | 对 `chart_id` 执行评估预测 |
| `/api/dataset-preview/categories/` | GET | 获取数据集类别 |
| `/api/dataset-preview/samples/` | GET | 按类别获取样例 |
| `/api/dataset-preview/select/` | POST | 选择数据集样例进入预览 |
| `/api/images/{filename}` | GET | 获取图像产物 |
| `/api/results/{filename}` | GET | 获取评估预测结果 |

## 9. 常见问题

### 上传时报模型失败

检查模型配置、网络、API key 和 `CHART_MODEL_PROFILE`。当前系统不会在模型失败时假装识别成功。

### 前端上传和数据集缓存效果不一样

自定义上传永远从 0 到 1 处理；数据集预览可能读取缓存。若需要完全一致，需要重新生成数据集预览缓存和评估预测缓存。

### 为什么某些密集图没有加密

系统会检查加密 label 的可放置空间。如果加密后 label 过密，整条数值轴会跳过加密，避免破坏图表可读性。

### 为什么评估预测没有 GT 误差

自定义上传通常没有 GT，因此前端主要展示系统预测结果和处理质量。只有离线评估或带 GT 的评估数据才计算误差指标。

## 10. 验证命令

```powershell
python -m py_compile backend/main.py backend/type_detection/chart_type.py backend/Grid_generation/grid_generation.py
cd frontend/chart-demo-ui
npm.cmd run build
```
