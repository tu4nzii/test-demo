## Pie/Donut 圆形流程校准（2026-07-01）

当前实验版 `pie` / `donut` 的 full flow 已统一为角度驱动流程，入口分别为：

```text
backend/evaluation_prediction/chart_modules/pie/runner.py
backend/evaluation_prediction/chart_modules/donut/runner.py
backend/evaluation_prediction/chart_modules/circular_flow.py
```

机制定义如下：

- `grid`: 固定 1 次。基于原始 `no_grid` 图生成 15°角度参考图；0°位于 12 点方向，角度顺时针递增，围绕外半径绘制角度网格并调用模型预测 `percentage/start_angle/end_angle`。
- `feedback`: 最多 2 次。第 1 轮在 grid 图上绘制 grid 预测的 start/end 边界与红色反馈弧；若 feedback1 与 grid 的 start/end 边界差异均不超过 `2°`，提前进入 amplifier。否则进入 feedback2；feedback2 后不再继续 feedback。
- `amplifier`: 最多 3 次。第 1 轮以结束轮 feedback 的预测角度裁剪，放大 `2x`，局部角度网格间隔 `5°`，start/end 各向外 padding `15°`。第 2/3 轮以上一轮 amplifier 预测角度裁剪，仍放大 `2x`，局部角度网格间隔 `2°`，start/end 各向外 padding `8°`。
- amplifier 每轮后都只和上一轮预测比较 start/end 角度；若两个边界差异均不超过 `2°`，提前停止。该停止条件只比较模型预测之间的一致性，不使用 GT/RNE。
- 若某对象的 grid 阶段没有得到可用 `start_angle/end_angle`，则不能生成基于预测结果的 feedback overlay，也不能生成基于预测结果的 amplifier crop；系统会记录该对象 full-flow 缺失，而不会用 GT 或 baseline 补齐生成端。
- coverage 审计已改为按 `(stage, round)` 的 distinct round 统计机制轮次；重复 modal call 仍保存在日志中并通过 `attempts`/call 记录复盘，但不会被误判为 feedback 或 amplifier 超轮次。

## Amplifier 坐标轴缩放边界（2026-07-01）

当前实验版按图表坐标结构区分 zoom-in / amplifier 的裁剪方式：
- `scatter` / `bubble` 是双数值轴图，amplifier crop 必须保持 x/y 等比例缩放，输出为方形局部坐标图，避免任一方向被非等比拉伸导致模型误读点中心。
- `h_bar` / `v_bar` / `line` 是“文字轴 + 数值轴”图，amplifier 的目标是找到更合适的数值刻度区间，因此只沿数值轴做局部缩放与局部 ruler 加密；文字轴只用于锁定类别/系列，不作为需要放大的测量轴。
- `h_bar` 的 crop 限制在 x 数值轴 plot 区间内，避免 y 文字轴标签进入局部图；`v_bar` 和 `line` 的 crop 限制在 y 数值轴 plot 区间内，避免 x 文字轴标签进入局部图。
- 以上调整仍然不改变机制：crop 中心来自上一轮模型预测，GT 只用于指标和日志，不用于裁剪中心或停止条件。
## Zoom-in / Amplifier 优化策略（2026-07-01）

本轮优化不改变论文机制：full flow 仍然是 `grid -> feedback -> amplifier`，baseline 仍为独立对照；amplifier 的输入仍由上一轮模型预测驱动，GT 只用于 RE/RNE 与日志复盘。优化范围仅限机制内部的图像输入质量、局部 guide 样式、裁剪窗口和提示词表达。

从 MLLM 的通用视觉编码特性出发，amplifier 图像遵循以下原则：
- 输出短边按模型视觉预算注册表选择，默认约 `768px`；避免 224/320px 局部图丢失 tick label、细边界和小 mark，也避免对小模型盲目放大浪费视觉 token。
- 裁剪窗口不再每轮简单减半，而是采用“先保上下文、再逐步收紧”的三轮策略：第 1 轮保留候选目标周围较宽上下文，第 2 轮中等局部化，第 3 轮只在已有稳定预测附近细化。
- 局部红色 guide 与 GT grid 样式严格区分：amplifier 使用红色局部 guide；GT grid 仍使用 `#cccccc`、2/2 dash、1px。
- 局部 ruler 使用更密的插值线，但 tick label 必须保持可读；line 图采用“密线、稀疏标签”，避免红线和标签遮挡浅色折线。
- amplifier 的局部 tick/angle/radius label 框必须以对应红色 guide 为中心轴：竖线穿过 label 框中心、横线穿过 label 框中心，极坐标圆环/射线也穿过 label 框中心；贴近裁剪边缘导致无法完整显示的 label 不强行绘制。
- prompt 明确要求模型读取原图数据坐标：使用最近两条红色 guide 做比例插值，不输出 crop 像素、resize 像素或标签位置。
- 对 scatter/bubble，点大小估计只作为 crop 尺寸和可读性辅助，不作为独立预测或额外 modal call；输出仍来自 amplifier 模型回答。

各类型当前策略：
- `h_bar` / `v_bar`: amplifier crop 输出按模型 profile 控制，局部红色 ruler 按轮次加密，窗口按预测边界周围保留足够条形边界上下文。
- `line`: amplifier crop 输出按模型 profile 控制，目标 x 位置用红色竖向 guide 标出，y 方向用较密红线和稀疏可读标签辅助插值。
- `scatter` / `bubble`: adaptive crop 输出按模型 profile 控制，局部红色 x/y 网格密度自适应，mark diameter 只影响 crop 尺寸和 guide 间距。
- `pie`: sector amplifier 图按模型 profile 放大并设最大边长，红色 start/end 边界带文本标注，帮助模型判断边界顺序。
- `donut`: 保留原有角度 crop 机制，但增加三轮 pad，上下文不再过早收窄；角度网格仍随轮次变细。
- `radar` / `rose`: polar amplifier 图按模型 profile 控制，径向窗口保留更多圆心/轴向上下文，局部红色圆环标注可见 r 值。

## 模型视觉预算注册表（2026-07-01）

按第一性原理判断，这个注册表是必要的：zoom-in/amplifier 的图像越大，不一定越准；对 MLLM 来说，大图会消耗更多视觉 token 和请求时长，密集红线还可能遮挡真实 mark。不同模型的视觉编码、OCR 能力和小目标识别能力不同，因此应该在不改变 `grid -> feedback -> amplifier` 机制的前提下，自适应选择局部图尺寸和 guide 密度。

当前实现位置：

```text
backend/evaluation_prediction/common/model_vision_registry.py
```

已注册论文模型：

| 模型 | point | ruler | sector/polar | max side | 设计取向 |
|---|---:|---:|---:|---:|---|
| GPT-4o | 768 | 640 | 768 | 1024 | 按 512 tile 档控制，ruler 图避免外部 label 多跨一档。 |
| Gemini 2.5 Flash | 768 | 640 | 768 | 768 | 按 768 tile 档控制，point 图吃满单 tile，ruler 图尽量不超过单 tile。 |
| claude-haiku-4.5 | 640 | 576 | 640 | 896 | 近似面积计 token，减少面积和局部红线密度。 |
| Intern VL3-78B | 896 | 672 | 896 | 896 | 按 448 tile 动态切图控制在 2x2 tile 级别。 |
| Pixtral-12B-2409 | 640 | 512 | 640 | 768 | 原生 16x16 patch，避免大面积 patch 浪费。 |

该注册表只影响 amplifier 阶段的 crop 输出尺寸、最大边长、label padding、三轮窗口收缩曲线、point grid density 和 polar window，不新增阶段、不新增 modal call、不使用 GT 生成预测。GT 实验返回 payload 会记录 `model_vision_profile`。`modal_call_records.csv` 会额外写入 `image_width`、`image_height`、`estimated_image_tokens` 和 `vision_profile`，用于审计视觉 token 消耗。

注意：zoom-in/amplifier 现在显式区分 `source ROI` 和 `output size`。`source ROI` 负责从原图中去掉多余对象，`output size` 负责把局部区域上采样到模型看得清的视觉预算。默认第 1/2/3 轮 source ROI 分别不超过数值 plot 区域约 `32% / 24% / 18%`；如果模型判断不可读，下一轮才扩大 ROI，但最大不超过约 `55%`。因此新的 zoom-in 不只是把大裁剪 resize 成小图，而是先做更局部的源图裁剪，再上采样。
## 实验版机制边界更新（2026-07-01）

当前 full flow 的最终结果只来自模型阶段输出：grid -> feedback -> amplifier。baseline 是独立基线，不作为后续阶段输入，也不参与 full-flow final selection。此前实验迁移中加入的本地图像读数辅助已经从结果链路移除；后续只能调整提示词、裁剪区域、缩放和过程图样式，不能增加论文机制之外的预测来源。

# GT 网格评估实验版项目入口

当前分支：`experiment/gt-grid-evaluation`。

本项目入口文档以 `项目文档.md` 为准：

```text
项目文档.md
架构.md
backend/evaluation_prediction/gt_experiment_flow_integrity_report_zh.md
```

其中：

- `项目文档.md`：实验版项目目标、边界、流程、输出目录和第一性原理审查结论。
- `架构.md`：各图表类型如何执行 baseline、grid、feedback、zoom-in/amplifier，以及模块拆分。
- `gt_experiment_flow_integrity_report_zh.md`：最近一次九类 synthetic 完整性检查报告。

实验版不再以上传、自动类型识别、自动网格生成和缓存复用为主流程。当前目标是基于 GT 类型、GT grid 图像、GT tick-pixel 映射和 GT 数据，真实调用模型评估第 3 步“评估预测”能力，并完整记录每次 modal call。

人工标注 GT grid 数据已整理到 `experiment_gt_dataset/organized/`。后端可用 `source=experiment_gt_real` 和 `source=experiment_gt_synthetic` 直接读取；这些样本的 `image_paths.grid_with_grid` 指向现成 `gt_grid/` 图像，不再依赖 fallback 绘制。

## Baseline 与 Full Flow 的边界

`baseline` 是独立基线方法，只用于和 full flow 做效果对比。它的预测会写入日志、CSV 和 `gt_metrics.json`，用于计算 baseline 自己的 RE/RNE。

但 baseline 不属于当前机制的 full flow，不能进入：

- `grid -> feedback -> amplifier` 的预测历史。
- feedback overlay 的标志绘制。
- amplifier crop 的裁剪中心。
- full-flow selected prediction 的 fallback。

full flow 的前序关系只能是：

```text
grid 预测 -> feedback 预测 -> amplifier 预测
```

如果 full flow 没有有效预测，应记录 full flow 缺失，不能用 baseline 结果顶替。

amplifier 的存在性判断合并进自身预测调用：目标可读则返回值；不可读则返回 `readable=false` 且预测值为 `null`，该轮计入 amplifier 次数，下一轮扩大 ROI 后继续，最多三轮。`attempts` 只表示网络/API 重试，不表示不可读后的重新裁剪。

scatter/bubble 的点大小判断也按同一原则合并进 amplifier：runner 不再为点直径或点存在性单独发起模型请求，而是从上一轮 feedback 坐标附近的 `no_grid` 图像像素确定性估计 `estimated_mark_diameter_px`，用于设置局部 crop 尺度，并把该尺寸作为同一次 amplifier 提示词里的目标识别/可读性线索。该信息记录在结构化结果和 modal call 的 `stage_support` 中，但不增加机制阶段，也不进入 baseline。

bar 类 amplifier 当前使用原图 `no_grid` 作为局部 crop 底图，再叠加红色局部虚线网格和底部 tick ruler；中列 GT grid 仍使用灰色 `#cccccc`、`2px 实线 + 2px 空白`、`1px` linewidth 的加密网格样式，两套样式不能混用。GT 实验运行时预测一致性阈值为 `CHART_PREDICTION_CONSISTENCY_TOLERANCE=0.0025`，只比较模型预测之间的一致性，不使用 GT/RNE。

最近一次真实 `h_bar_001` 运行目录：

```text
backend/evaluation_prediction/results/gt_experiments/h_bar/h_bar_001/gemini-2.5-flash-lite__h_bar_001
```

该次运行 `modal_calls.jsonl=16`、结构化预测记录 `16`，`call_id` 全部匹配；最终 full-flow selected predictions 来自 amplifier，平均 MAE/RE 为 `0.002 / 0.0080333333`，优于 baseline 的 `0.0033333333 / 0.0139`。

每次 modal call 的人工可读扁平记录统一保存为：

```text
<run_dir>/modal_call_records.csv
```

该 CSV 使用 `utf-8-sig` 编码，包含图表名称、处理对象、对象类别、GT、按图表类型拆分后的预测值、`attempts`、输入 token、输出 token、文字提示词 token、单轮请求耗时、RE/RNE 和 `call_id`。完整原始输入输出仍保存在：

```text
<run_dir>/modal_calls_full_enriched.jsonl
```

## Feedback Final Overlay 颜色含义

`final_overlay_*_feedback_grid_with_grid_runN.png` 只用于展示 feedback 阶段的模型预测轨迹，不展示 baseline、grid 或 amplifier 预测。

颜色按 feedback 轮次排列：

| 颜色 | 含义 |
| --- | --- |
| 红色 red | feedback 第 1 轮预测 |
| 紫色 purple | feedback 第 2 轮预测 |

因为实验版 feedback 最多 2 次，所以新的 final overlay 最多只应出现两个 feedback 标志。若旧 run 目录中的 final overlay 出现三个标志，那是旧绘制逻辑把 grid 初始预测也一起画进了历史轨迹，并不表示 feedback 跑了 3 次。

## 2026-07-01 全类型迁移校验


最新 synthetic smoke 结果如下，9 类 tested object 的 full flow final 在 RE 和 RNE 上均优于 baseline，且 stage call violation 均为 0：

| 类型样本 | baseline RE | final RE | baseline RNE | final RNE | violation |
|---|---:|---:|---:|---:|---:|
| `bubble_023` | 62.323793 | 0.235965 | 389.227384 | 0.049996 | 0 |
| `scatter_001` | 0.028367 | 0.021453 | 0.026964 | 0.021738 | 0 |
| `line_001` | 325000000000.008 | 130952380952.382 | 0.010500 | 0.003218 | 0 |
| `v_bar_002` | 0.003806 | 0.003285 | 0.003050 | 0.002610 | 0 |
| `h_bar_001` | 0.013889 | 0.008038 | 0.005556 | 0.003333 | 0 |
| `pie_001_no_grid` | 0.037500 | 0.002095 | 0.016667 | 0.000931 | 0 |
| `donut_000` | 0.509015 | 0.038742 | 0.045556 | 0.003122 | 0 |
| `radar_000` | 0.586515 | 0.037957 | 0.459375 | 0.021587 | 0 |
| `rose_000` | 0.132893 | 0.043464 | 0.186667 | 0.060304 | 0 |

注意：`line_001` 含 GT 为 0 的点，因此 RE 会被零值分母放大；该类论文对比应重点同时查看 RNE。

输出复盘文件也已统一：九类最新目录均包含 `full_flow_final_predictions.csv`、`full_flow_final_selection_audit.csv`、`modal_calls.jsonl`、`modal_calls_full.jsonl` 和 `modal_calls_enriched.jsonl`。

## 2026-07-01 当前实验结果快照

已重新扫描并生成当前结果汇总：

```text
backend/evaluation_prediction/results/gt_experiments/current_experiment_results.md
backend/evaluation_prediction/results/gt_experiments/current_experiment_results.csv
```

当前扫描到 `134` 个对象级最新 run。汇总文件每个图表对象只取最新一次 run；`final avg RNE` 只统计已有 full-flow final 的对象，不能把旧 run 中只有 baseline 的对象误读为 full flow 已完整完成。

| 类型 | 图表数 | 有 final 对象 | final 优于 baseline | final avg RNE | baseline avg RNE | 缺失 full-flow 对象 | 轮次违规对象 |
|---|---:|---:|---:|---:|---:|---:|---:|
| bubble | 4 | 2 | 1 | 0.0555 | 97.3690 | 0 | 0 |
| donut | 3 | 2 | 2 | 0.0040 | 0.0285 | 7 | 0 |
| h_bar | 53 | 11 | 11 | 0.0201 | 0.0623 | 3 | 2 |
| line | 2 | 2 | 2 | 0.0217 | 0.0307 | 2 | 0 |
| pie | 2 | 2 | 2 | 0.0016 | 0.0605 | 2 | 0 |
| radar | 1 | 1 | 1 | 0.0216 | 0.4594 | 8 | 0 |
| rose | 1 | 1 | 1 | 0.0603 | 0.1867 | 3 | 0 |
| scatter | 2 | 2 | 1 | 0.0776 | 0.0263 | 0 | 0 |
| v_bar | 66 | 3 | 2 | 0.0214 | 0.0178 | 7 | 3 |

当前解读：

- `h_bar`、`line`、`pie`、`donut`、`radar`、`rose` 的已验证对象整体优于 baseline，但 donut/radar/rose 仍有对象级 full-flow 缺失，需要继续补齐完整输出。
- `scatter` 当前有 1 个对象 final RNE 劣于 baseline，主要需要继续优化双数值轴 crop 尺寸、局部 guide 密度和提示词，而不能引入机制外读数。
- `bubble` 当前有 1 个真实对象 final RNE 劣于 baseline，后续应优先检查点大小辅助是否让 crop 过窄或目标上下文不足。
- `v_bar` 当前仅 3 个对象有 full-flow final，其中 1 个劣于 baseline，且存在 3 个轮次违规对象；后续应优先重跑/审查 `GroupedBarChart10` 及同类 grouped bar。
- RE 在 GT 接近 0 时会异常放大，论文结果对比应以 RNE 为主，同时保留 RE 作为补充。

## 2026-07-01 全类型真实调用抽样复测

本轮按当前机制真实调用 `gemini-2.5-flash-lite`，为 9 个图表类型各选取一个可用对象进行 baseline 与 full-flow 对比。真实 GT 数据集覆盖 `bubble/scatter/line/v_bar/h_bar/radar/rose`；当前整理后的真实 GT 数据集暂未覆盖 `pie/donut`，因此这两类使用旧 synthetic 源补齐。

本轮代码调整只发生在机制内部：

- h_bar/v_bar/line 的 full-flow final selection 增加“amplifier 离群回退”：当 grid 与 feedback 已经稳定，而 amplifier 序列中位数相对稳定预测偏离超过数值轴跨度约 `6%` 时，回退到稳定的 grid/feedback；该判断只比较预测之间的距离，不使用 GT。
- pie/donut 的 full-flow 提示改为以 `percentage` 作为主读数，`start_angle/end_angle` 作为 feedback 与 amplifier 裁剪辅助，避免圆形图被角度补集误差主导。
- pie/donut 取消 `single_missing_remainder` / `amplifier_pct` 等机制外最终值来源；final 只来自真实 grid/feedback/amplifier 模型阶段输出。

本轮 9 类选中样本的 RNE 对比如下：

| 类型 | 样本 | final RNE | baseline RNE | final 优于 baseline | coverage 备注 |
|---|---|---:|---:|---|---|
| bubble | `BubbleChart10` | 0.016390 | 0.036000 | 是 | stage coverage 干净 |
| scatter | `ScatterPlot1` | 0.024063 | 0.025625 | 是 | stage coverage 干净 |
| line | `LineGraph1` | 0.055562 | 0.084312 | 是 | 有 1 个对象 amplifier 有效输出缺失，但 final RNE 优于 baseline |
| v_bar | `BarChart12` | 0.147436 | 0.166333 | 是 | stage coverage 干净 |
| h_bar | `BarChart1` | 0.016667 | 0.043367 | 是 | 有 1 个对象 amplifier 有效输出缺失，离群回退后 final 优于 baseline |
| pie | `pie_006_no_grid` | 0.194600 | 0.252000 | 是 | 有 1 个对象有效阶段输出缺失；`pie_001/003/005` 仍是失败样本 |
| donut | `donut_007` | 0.020000 | 0.040000 | 是 | RNE 通过，但 5 个扇区未进入完整 feedback/amplifier，需继续优化 donut grid 可读性 |
| radar | `RadarChart15` | 0.139087 | 0.198413 | 是 | RNE 通过；coverage 中存在 polar grid 重复计数/部分 amplifier 无效输出，需要继续清理审计口径 |
| rose | `Rose1` | 0.160000 | 0.266667 | 是 | RNE 通过；coverage 中存在 polar grid 重复计数/部分对象后续阶段缺失，需要继续审查 |

结论：本轮已经找到每个图表类型至少一个 full-flow final RNE 优于 baseline 的真实调用输出。严格流程审计上，`bubble/scatter/v_bar` 最干净；`line/h_bar/pie` 有少量阶段有效输出缺失但阶段链路基本可复盘；`donut/radar/rose` 的 RNE 结果可用，但 coverage 仍需作为后续优先维护点，尤其 donut 不应让 grid 无效对象直接跳过 feedback/amplifier。
